# 2025 年高教社杯全国大学生数学建模竞赛 C 题
# 《NIPT 的时点选择与胎儿的异常判定》完整建模方案
## ——统计决策主线与选择性机器学习审计终稿

> **方案定位**：本方案面向国赛论文和代码落地，强调统计单位正确、跨问接口真实、外推边界清楚、最终结果可复算。  
> **主线原则**：统计模型负责解释、估计和形成最终决策；机器学习只在完全相同的数据划分和指标下检查非线性增益，未通过晋级门槛时不得改变最终结论。  
> **真实性说明**：文中标为“附件复核”或“快速试算”的数字来自所给 Excel；标为“最终应输出”的项目仍需按完整流程重新计算，不把路线核验值冒充最终竞赛答案。  
> **数据边界**：附件以某地区、偏高 BMI 人群为主；模型的稳健性指对抽样、误差、阈值和分布扰动稳定，不等同于未经外部队列验证的临床普适性。

---

# 0. 方案总览

## 0.1 一句话主线

本题的核心不是给四个问题各配一个算法，而是先从重复检测中分离“孕妇个体差异”和“测量误差”，再把 Y 浓度规律转化为可用于决策的可靠达标时点分布，进而完成 BMI 分组与多因素稳健化；女胎问题则在同一“质量—概率—保守决策”框架下完成多标签异常判定。

全文主干为：

\[
\boxed{
\text{记录—采血事件—孕妇三级重构}
\rightarrow
\text{纵向关系与误差识别}
\rightarrow
\text{首次可靠达标区间}
\rightarrow
\text{AFT 时点分布}
\rightarrow
\text{BMI 精确分组}
\rightarrow
\text{多因素增量与稳健决策}
}
\]

女胎分支为：

\[
\boxed{
\text{非冗余测序质量表征}
\rightarrow
\text{三条染色体异常概率}
\rightarrow
\text{正常/需复检/异常三段式输出}
}
\]

## 0.2 最终推荐模型组合

| 问题 | 主模型 | 最终决策模型 | 备选/校验 | 机器学习角色 |
|---|---|---|---|---|
| 问题一 | 组间/组内 BMI 分解的简洁混合效应模型 | 不单独优化，只输出效应与误差 | GAMM 或单非线性块混合模型 | 随机森林只检查遗漏非线性 |
| 问题二 | 误差感知的区间删失 log-normal AFT | 可靠度约束下的精确动态规划 | log-logistic、Weibull、纵向重构/Turnbull | 不进入最终分组 |
| 问题三 | 与问题二嵌套的多因素 AFT | 配对增量检验 + 尾部风险复核 + 必要时重分组 | BMI-only 冻结基线、区间删失 Cox 备用 | 只用于证明是否存在稳定增量 |
| 问题四 | 非冗余质量门控 + 三个正则逻辑/Firth 模型 | 概率区间与拒判机制 | Z 规则基线 | RF 只在 T13/T18 局部申请晋级；T21 禁止树模型强判定 |

## 0.3 为什么不继续增加模型

附件快速复核已经表明：

- 三种常见 AFT 分布的 AIC 几乎相同，代表 BMI 的 90% 时点差异约在 0.5 周内；
- 问题一随机森林相对 Ridge 的 RMSE 仅改善约 1.35%，外样本解释度仍很低；
- 问题三多因素随机森林的 Brier 改善区间覆盖 0；
- 问题四只有 T13 显示较明显的局部非线性潜力，T18 指标互有胜负，T21 树模型不稳定。

因此，提升答案准确性的优先级应是：

1. 修正技术重复定义；
2. 正确处理阈值上下反复穿越；
3. 保证动态规划输出满足覆盖约束；
4. 对高 BMI 尾部设置拒绝强外推机制；
5. 固定孕妇级验证折，完成配对比较；
6. 最后才考虑增加模型复杂度。

---

# 1. 题意覆盖与任务图

## 1.1 四问任务台账

| 问题 | 题目要求 | 数学本质 | 精确输出 | 对后问的作用 |
|---|---|---|---|---|
| 问题一 | 分析男胎 Y 浓度与孕周、BMI 等指标的相关特性，建模并检验显著性 | 比例型纵向重复测量回归 | 关系方向、非线性形态、系数/效应曲线、显著性、个体差异、误差估计 | 提供可信状态口径、变量方向和 Q2 校验曲线 |
| 问题二 | 仅按 BMI 合理分组，给出区间和最佳 NIPT 时点，使潜在风险最小，并分析检测误差 | 区间删失时点估计 + 一维连续分段决策 | BMI 切点、组样本数、推荐孕周、可靠度/覆盖率、置信区间、误差影响 | 成为 Q3 的冻结基线与比较对象 |
| 问题三 | 综合身高、体重、年龄等因素、检测误差和达标比例，仍按 BMI 分组并给出最佳时点 | 嵌套多因素事件时间模型 + 稳健群体压缩 | 增量价值、修订/保留的切点、组时点、平均与尾部风险 | 形成最终男胎推荐方案 |
| 问题四 | 以女胎 AB 列为结果，综合 Z 值、X 浓度、GC、读段、BMI 等给出异常判定方法 | 小阳性、重复孕妇、多标签、质量感知选择性分类 | 正常/需复检/异常、异常染色体、概率/区间、操作建议 | 与男胎共同形成质量不足时先复检的统一规则 |

## 1.2 题目给定条件与新增假设分离

### 题目明确给定

- 男胎 Y 染色体浓度达到或超过 4% 时，NIPT 结果通常较可靠；
- 孕期一般在 10—25 周可以检测；
- 12 周以内发现风险较低，13—27 周风险较高，28 周以后风险极高；
- 男胎 Y 浓度与孕周、BMI 等因素有关；
- 女胎不含 Y 染色体，需用 13/18/21/X 相关指标和测序质量判定；
- 附件可能存在多次采血、多次检测或同次采血多次检测；
- AB 列是问题四指定的异常判定结果。

### 本方案新增、必须检验的假设

1. 同一“孕妇代码 + 抽血次数 + 孕周”代表同一采血事件；检测日期可因复测而不同；
2. 技术复测误差在主分析中近似独立、零均值，方差可由重复组估计；
3. BMI 越高，可靠达标时点不应系统性提前；该单调性需由数据方向和敏感性支持；
4. AFT 主模型在观测支持域内可以近似描述可靠达标时间；
5. 问题四的 AB 是记录级实验室标签，不是婴儿出生结局；
6. 新孕妇的推荐时点只能使用决策前可获得的变量；
7. 超出样本支持域时允许输出“无法保证/建议个体化复检”，不强行给点估计。

## 1.3 中央矛盾

男胎时点决策同时面临：

\[
\text{过早检测}
\Rightarrow
P(Y<0.04)\text{ 较高、结果不稳定},
\]

\[
\text{过晚检测}
\Rightarrow
\text{治疗窗口缩短、潜在风险增加}.
\]

女胎异常判定同时面临：

\[
\text{阈值过松}
\Rightarrow
\text{误报增加},
\qquad
\text{阈值过严}
\Rightarrow
\text{漏报增加},
\]

且低质量样本不应被强制二分类。由此，全文统一决策原则为：

\[
\boxed{
\text{先满足可靠性/质量约束}
\rightarrow
\text{再选择最早或最明确的决策}
\rightarrow
\text{无法可靠判断时允许拒判}
}
\]

---

# 2. 附件数据复核与关键事实

## 2.1 数据层级

| 数据集 | 原始记录 | 唯一孕妇 | 主口径采血访视 | 主要结构 |
|---|---:|---:|---:|---|
| 男胎 | 1082 | 267 | 1022 | 纵向重复测量 + 同次采血技术复测 |
| 女胎 | 605 | 147 | 记录级建模、孕妇级划分 | 多标签、类别不平衡、重复检测 |

男胎技术重复组共 39 组：18 组有 2 条检测，21 组有 3 条检测。若错误地把检测日期也加入访视键，则会得到 1064 个访视、18 个技术重复组，说明“检测日期是否属于访视定义”会实质影响误差估计，必须做双口径敏感性。

## 2.2 Y 浓度、孕周与 BMI 的快速统计

原始男胎记录复核得到：

\[
r_{Pearson}(Y,BMI)=-0.15130,
\qquad
p=5.74\times10^{-7},
\]

\[
r_{Pearson}(Y,GA)=0.12656,
\qquad
p=2.98\times10^{-5}.
\]

Spearman 相关分别约为 -0.15496 和 0.08429。简单线性回归为：

\[
\widehat Y
=0.119461
-0.001964\,BMI
+0.001255\,GA,
\qquad
R^2=0.04568.
\]

这些结果只作描述性方向检查。由于同一孕妇多次出现，原始行级 p 值不能作为正式显著性结论。

快速随机截距模型得到：

\[
\widehat{ICC}=0.73876,
\qquad
\widehat\sigma_{res}=0.01712.
\]

约 74% 的条件相关波动来自孕妇间持久差异或孕妇内聚类，说明把 1082 行当作独立样本会严重低估标准误。

## 2.3 技术误差

以同一采血事件内的重复 Y 浓度估计合并组内方差：

\[
\widehat\sigma_{tech}
=\sqrt{
\frac{
\sum_g\sum_{r=1}^{R_g}
(Y_{gr}-\bar Y_g)^2
}{
\sum_g(R_g-1)
}
}
=0.0061265.
\]

重复测量绝对差的中位数约为 0.004702。4% 阈值为 0.04，技术误差相当于约 0.61 个百分点，不可忽略。

若使用“孕妇 + 日期 + 抽血次数 + 孕周”的严格键，得到的技术误差约为 0.004734。主分析使用 0.00613，严格键作为较低误差情景。

## 2.4 首次达标的删失结构

同次采血技术复测取中位数后，按 4% 硬阈值构造初始事件区间：

| 类型 | 人数 | 比例 | 含义 |
|---|---:|---:|---|
| 左删失 | 217 | 81.27% | 第一次观测已经达标，真实首次达标更早 |
| 区间删失 | 43 | 16.10% | 前一次未达标、后一次达标 |
| 右删失 | 7 | 2.62% | 最后一次仍未达标 |

重左删失意味着：

- 不能把第一次阳性孕周当成精确达标时间；
- 早期时点的形状依赖分布假设，必须报告分布敏感性；
- 任何深度学习或普通回归若把“第一次阳性记录”作为标签，都会系统性偏晚。

## 2.5 阈值回落与事件语义

硬阈值状态中：

- 32 名孕妇曾经达到 4%，随后又低于 4%；
- 28 名孕妇出现两次以上状态切换；
- 使用纯技术误差构造 95% 可信状态后，仍有 3 名孕妇出现可信状态冲突。

因此，本文不把每条观测的 \(Y\ge0.04\) 直接当作吸收态，而定义“首次可靠达标时点”：阈值附近记录先标记为不确定，再由可信未达标与可信达标构造区间；对可信冲突者使用宽区间、单调隐状态或排除敏感性。

## 2.6 BMI 支持域

男胎首次 BMI 的关键分布为：

| 统计量 | BMI |
|---|---:|
| 最小值 | 20.703 |
| 5% 分位 | 28.271 |
| 中位数附近 | 约 31.85 |
| 95% 分位 | 37.176 |
| 最大值 | 46.875 |

人数分布：

- BMI \(\ge36\)：20 人；
- BMI \(\ge40\)：4 人；
- BMI \(\ge42\)：2 人。

因此，36 以上已是稀疏尾部，40 以上只能给高不确定性结论；不允许把树模型在尾部的平坦预测解释成“高 BMI 反而提前达标”。

## 2.7 女胎标签与质量变量

女胎 605 条记录中：

- AB 为空 538 条，至少一个异常标签 67 条；
- T13 阳性 23 条，来自 18 名孕妇；
- T18 阳性 46 条，来自 30 名孕妇；
- T21 阳性 13 条，来自 12 名孕妇；
- 44 名孕妇至少有一次异常记录；
- 43 名孕妇在不同记录上的 AB 状态不完全一致；
- “胎儿是否健康”全部为“是”，不能作为独立临床真值。

AB 可出现 T13T18、T18T21 等组合，所以问题四是多标签分类，不是互斥多分类。

质量变量中，原始读段数与唯一比对读段数的 Spearman 相关约为 0.992，不能在质量分中双重加权。质量层只保留一个深度量，并将其余比对率、重复率、过滤率和 GC 偏离作为独立信息。

对固定 \(Z_k\ge3\) 规则做附件核验，记录级灵敏度为：

| 标签 | AB 阳性记录 | \(Z_k\ge3\) 命中 | 记录级灵敏度 | AB 阳性记录中的 \(Z_k\) 范围 |
|---|---:|---:|---:|---|
| T13 | 23 | 1 | 0.0435 | [-2.807, 3.808] |
| T18 | 46 | 0 | 0 | [-0.904, 2.974] |
| T21 | 13 | 0 | 0 | [-2.394, 1.329] |

改用 \(\lvert Z_k\rvert\ge3\) 也未提高上述灵敏度；按每名孕妇等权汇总时约为 0.032、0、0。这一异常不应被悄悄忽略，正式建模前必须再次核验 AB 语义、字段对齐和标签解析。若字段无误，它说明 AB 并非当前记录单一 Z 值的机械阈值标签，固定 Z 规则只能作为临床式参照，不能作为数据集内的安全回退分类器。

---

# 3. 整题建模主线与跨问联动

## 3.1 问题角色

| 问题 | 角色 | 新增信息 | 不应做的事 |
|---|---|---|---|
| 问题一 | 基础解释与误差识别 | 纵向效应、个体差异、技术误差 | 直接用行级相关替代正式模型 |
| 问题二 | 单因素决策 | BMI-only 达标分布和分组 | 把第一次阳性当精确事件；用 GA 求一维分割 |
| 问题三 | 增量与稳健扩展 | 多因素修正、达标比例、尾部风险 | 为“显得高级”强迫所有变量改变分组 |
| 问题四 | 并行分类分支 | 女胎质量与多标签概率 | 强行接收男胎 AFT 参数；低质样本硬分类 |

## 3.2 跨问题联动链

```mermaid
flowchart TD
    A[Excel 原始记录] --> B[孕妇-采血事件-检测记录三级索引]
    B --> C[同次采血复测合并]
    C --> D[技术误差 sigma_tech]
    B --> E[首次 BMI 与决策前协变量]

    C --> Q1[问题1 混合效应纵向模型]
    E --> Q1
    Q1 --> F[孕周/BMI效应方向]
    Q1 --> G[个体差异与纵向达标概率]
    D --> H[可信未达标/不确定/可信达标]
    F --> H
    H --> I[首次可靠达标删失区间]

    I --> Q2[问题2 BMI-only 区间删失 AFT]
    E --> Q2
    Q2 --> J[F2(t|BMI) 与个体可靠时点]
    J --> K[精确动态规划]
    K --> L[Q2 BMI 切点与组时点]

    I --> Q3[问题3 嵌套多因素 AFT]
    E --> Q3
    L --> Q3
    Q3 --> M[增量检验与尾部风险]
    M --> N[保留或修订 Q2 分组]

    B --> O[女胎 AB 多标签]
    B --> P[非冗余质量变量]
    P --> Q4[问题4 正则逻辑/Firth]
    O --> Q4
    Q4 --> R[正常/需复检/异常]

    G -.纵向重构校验.-> Q2
    D -.共享测量可靠性思想.-> P
    Q1 -.RF 非线性审计.-> S[选择性机器学习审计]
    Q3 -.多因素增量审计.-> S
    Q4 -.T13/T18 RF 局部候选.-> S

    N --> V[孕妇级 Bootstrap 与适用域审计]
    R --> V
    S --> V
    V --> Z[最终分组表/时点表/异常规则/区间/边界]
```

## 3.3 真正传递的接口

### 问题一传给问题二

- \(\widehat\sigma_{tech}\)：构造可信状态；
- BMI 与孕周的效应方向：约束 AFT 的合理方向；
- 首次 BMI 与后续 BMI 变化的区分：防止未来信息泄漏；
- 纵向模型的 \(P(Y(t)\ge0.04)\)：校验 AFT 的 \(F(t)\)。

### 问题二传给问题三

- 完全冻结的 BMI-only 删失区间、AFT 分布和外层折；
- \(F_2(t\mid BMI)\)；
- Q2 切点、组时点和风险；
- Q3 必须用同一指标证明增量，而不能改变口径获取表面优势。

### 前三问与问题四的共享接口

- 孕妇是最小独立验证单位；
- 测量质量差时允许拒判；
- 所有阈值和预处理只在训练折确定；
- 结果输出概率、区间和操作建议，而不是只有类别。

---

# 4. 全文统一建模口径

## 4.1 索引与符号

| 符号 | 含义 | 单位/取值 | 使用问题 |
|---|---|---|---|
| \(i=1,\ldots,n\) | 孕妇 | 孕妇代码 | 全部 |
| \(v=1,\ldots,m_i\) | 第 \(i\) 名孕妇的采血访视 | 抽血事件 | Q1—Q3 |
| \(r=1,\ldots,R_{iv}\) | 同一采血事件的技术复测 | 检测记录 | Q1—Q3、质量层 |
| \(k\in\{13,18,21\}\) | 目标常染色体 | 标签索引 | Q4 |
| \(t_{iv}\) | 检测孕周 | 周 | Q1—Q3 |
| \(Y_{ivr}^{obs}\) | 观测 Y 染色体浓度 | 比例 | Q1—Q3 |
| \(Y_{iv}^{*}\) | 潜在真实 Y 浓度 | 比例 | Q1—Q3 |
| \(B_{iv}\) | 访视 BMI | kg/m\(^2\) | Q1 |
| \(B_{i0}\) | 首次可用 BMI | kg/m\(^2\) | Q1—Q3 |
| \(\Delta B_{iv}=B_{iv}-B_{i0}\) | 孕期内 BMI 变化 | kg/m\(^2\) | Q1 |
| \(T_i^{rel}\) | 首次可靠达到 4% 的潜在孕周 | 周 | Q2—Q3 |
| \((L_i,R_i]\) | \(T_i^{rel}\) 的删失区间 | 周 | Q2—Q3 |
| \(F_q(t\mid x)\) | 问题 \(q\) 中孕周 \(t\) 前已可靠达标的概率 | [0,1] | Q2—Q3 |
| \(\rho\) | 统一政策保护水平，主报告 0.8/0.9 | 概率 | Q2—Q3 |
| \(\tau_i(\rho)\) | 个体达到可靠度 \(\rho\) 的最早时点 | 周 | Q2—Q3 |
| \(G_g=[c_{g-1},c_g)\) | 第 \(g\) 个 BMI 组 | kg/m\(^2\) | Q2—Q3 |
| \(t_g\) | 第 \(g\) 组推荐时点 | 周 | Q2—Q3 |
| \(A_{ivr,k}\) | 女胎记录的第 \(k\) 条染色体异常标签 | 0/1 | Q4 |
| \(p_{ivr,k}\) | 第 \(k\) 条染色体异常概率 | [0,1] | Q4 |
| \(D_{Q,ivr}\) | 测序质量异常距离 | 非负 | Q4 |

## 4.2 单位和时间规则

- “11w+6”转换为 \(11+6/7\) 周；
- 计算时保留至少 4 位小数；
- 最终时点转换为“周+天”，按最近整天输出；
- BMI 切点计算时保留原精度，落地时取 0.5 或整数，并回代验证；
- 题目通常检测窗口为 10—25 周，模型允许利用晚于 25 周的记录识别尾部，但最终若 25 周内无法保证可靠度，必须显式输出“窗口内无法保证”。

## 4.3 决策前信息原则

用于首次 NIPT 决策的变量只能来自决策时已经可获得的信息：

- BMI 使用首次检测或孕前/首诊 BMI；
- 年龄、身高、IVF、孕次、产次使用首次记录；
- 后续体重、后续测序质量、未来检测结果不得反向用于第一次时点；
- Q1 可以研究访视 BMI 变化，但 Q2/Q3 不能用全程平均 BMI；
- 所有缺失插补、标准化、特征筛选、阈值和校准器均只在训练折拟合。

## 4.4 统一验证单位

- 外层划分：按孕妇代码；
- Bootstrap：按孕妇有放回抽样，保留其全部记录；
- Q4 记录级评价时，每名孕妇总权重相同；同一采血事件多次检测时，该访视总权重也不得因复测次数增加；
- 固定外层折编号并导出，所有模型使用同一折做配对比较。

---

# 5. 数据预处理与质量控制算法

## 5.1 孕周解析

将字符串统一转换：

\[
GA(w,d)=w+\frac d7.
\]

检查规则：

1. 天数 \(d\in\{0,\ldots,6\}\)；
2. 孕周应落入数据合理范围；
3. 检测日期与末次月经推算孕周只作一致性审计，不因小差异自动删行；
4. 发现明显冲突时保留原题孕周为主，另做日期推算敏感性。

## 5.2 采血事件与技术复测

主访视键定义为：

\[
K_{iv}
=(\text{孕妇代码},\text{检测抽血次数},\text{检测孕周}).
\]

理由：题面说明可能“一次采血多次检测”；附件中相同抽血次数和孕周的记录可出现在不同检测日期，符合复测特征。

同一访视内的 Y 浓度主聚合采用中位数：

\[
\widetilde Y_{iv}
=\operatorname{median}_{r}Y_{ivr}^{obs}.
\]

同时保存：

- 重复次数 \(R_{iv}\)；
- 均值、极差和样本方差；
- 检测日期跨度；
- 每次检测的读段与 GC 质量。

若重复组内最大差异超过 \(3\widehat\sigma_{tech}\)，标记为“复测冲突”，主分析仍用中位数，敏感性改用 Huber 均值或逐条测量误差似然。

严格敏感性键为：

\[
K_{iv}^{date}
=(\text{孕妇代码},\text{检测日期},\text{检测抽血次数},\text{孕周}).
\]

## 5.3 技术误差估计

观测模型：

\[
Y_{ivr}^{obs}
=Y_{iv}^{*}+e_{ivr},
\qquad
e_{ivr}\sim N(0,\sigma_{tech}^2).
\]

合并组内方差：

\[
\widehat\sigma_{tech}^2
=
\frac{
\sum_{iv:R_{iv}>1}
\sum_r(Y_{ivr}^{obs}-\bar Y_{iv})^2
}{
\sum_{iv:R_{iv}>1}(R_{iv}-1)
}.
\]

正式实现还应检查：

- 误差是否随 Y 水平、孕周、BMI 或读段质量变化；
- 复测差的 Q-Q 图；
- 对高/低读段组分别估计误差；
- 主口径与含日期严格口径的差异。

若异方差明显，可令：

\[
\log\sigma_{tech,iv}^2
=\theta_0+\theta_1\log O_{iv}
+\theta_2|GC_{iv}-0.5|,
\]

否则使用全局 \(0.00613\) 并通过误差倍数敏感性传播。

## 5.4 缺失与结构性空值

- 女胎 Y 相关列为空是性别决定的结构性缺失，不插补；
- 女胎 1 条 BMI 缺失，只在训练折用中位数或多重插补；
- 男胎末次月经缺失不影响以题目孕周为主的分析；
- X 浓度可能为负，是估计量允许的结果，不截断；
- AB 空白表示题目给定的“无异常”，不是普通缺失；
- 怀孕次数等字符型变量先建立明确字典，再编码，未知类别保留缺失指示。

## 5.5 共线性处理

BMI、身高、体重近似满足：

\[
BMI=\frac{Weight}{(Height/100)^2}.
\]

因此：

- Q1 主模型使用首次 BMI、访视 BMI 变化和身高候选；
- Q3 主模型使用 BMI + 身高，体重只进入替代参数化；
- Q4 临床变量可在正则化下使用 BMI 与身高，但不把三者同时作为未正则线性项解释系数；
- 质量层不同时重复使用原始读段数和唯一比对读段数。

## 5.6 异常值原则

不因变量偏离常见范围就直接删除。采用：

1. 物理不可能值：更正或排除；
2. 测序质量异常：保留并作为质量信息；
3. 高杠杆孕妇：保留主分析，做逐孕妇影响诊断；
4. 高 BMI 稀疏尾部：保留估计，但输出宽区间和适用域警告；
5. 复测冲突：保留并进入测量误差敏感性。

## 5.7 固定数据划分

建议先生成并保存：

```text
fold_id.csv
孕妇代码,外层折,重复种子,数据集
A001,1,20250904,male
...
B001,2,20250904,female
```

任何模型都不能自行重新随机划分。正式结果同时保存逐孕妇/逐记录外层预测，保证配对 Bootstrap 可以复算。

---

# 6. 问题一：Y 浓度与孕周、BMI 等因素的关系

## 6.1 问题概述

### 任务

分析男胎 Y 染色体浓度与孕周、BMI 等指标的相关特性，建立关系模型并检验显著性。

### 已知

- 男胎 1082 条检测记录、267 名孕妇；
- 主口径合并后 1022 个采血访视；
- 同一孕妇存在纵向重复，部分访视存在技术复测；
- Q1 可使用访视 BMI 研究组内变化，但后续首次检测决策只能使用基线信息。

### 输出

- 孕周与 Y 的效应方向及非线性形态；
- 首次 BMI 与 Y 的组间效应；
- 孕期 BMI 变化的组内效应；
- 交互是否必要；
- 固定效应显著性、个体差异、残差与预测区间；
- 供问题二使用的 \(\widehat\sigma_{tech}\)、变量方向和纵向达标概率。

### 数学本质

这是比例型响应的纵向重复测量回归。核心困难不是普通相关系数，而是：

1. 同一孕妇多次检测相关；
2. Y 浓度随孕周可能非线性；
3. BMI 同时包含孕妇间差异和孕期内变化；
4. 技术误差与生物波动必须区分；
5. 新孕妇预测不能使用其未知随机效应。

## 6.2 总体求解思路

完整路径为：

1. 合并同次采血技术复测并估计技术误差；
2. 将 Y 浓度做 logit 变换；
3. 把 BMI 分解为首次 BMI \(B_{i0}\) 与组内变化 \(\Delta B_{iv}\)；
4. 先拟合随机截距线性基线；
5. 按证据逐步增加一个非线性块、交互或随机斜率；
6. 使用训练折内 BIC 与孕妇级外样本 RMSE/覆盖率选型；
7. 用整流程簇 Bootstrap 给系数、效应曲线和结点不确定性；
8. 输出 \(P(Y^*(t)\ge0.04\mid x)\)，用于问题二校验 AFT；
9. 随机森林只在同一外层折检查统计模型是否遗漏明显非线性。

问题一向问题二传递的是“误差、方向和校验概率”，而不是把混合模型的所有参数生硬塞进 AFT。

## 6.3 可用模型及选型比较

| 可行模型/思路 | 模型本质与核心变量 | 完整实现步骤 | 所需数据与假设 | 优点 | 局限与失败风险 | 验证方法 | 与前后问题的接口 | 适用场景 |
|---|---|---|---|---|---|---|---|---|
| OLS/GEE + 样条 | 总体平均的孕周/BMI 效应 | 合并访视；构造样条；按孕妇聚类稳健标准误 | 只要求均值结构正确 | 简单，适合作为基线 | OLS 忽略聚类；GEE 不直接给个体方差 | 聚类稳健 SE、外样本 RMSE | 给出总体效应基准 | 随机效应不稳定或只关心总体均值 |
| **简洁混合效应模型** | 固定效应解释孕周/BMI，随机效应解释孕妇差异 | BMI 组间/组内分解；从随机截距开始；按证据加一个非线性块和随机斜率 | 随机效应近似正态；残差经变换后可接受 | 解释完整、适配重复测量、可输出个体差异 | 结点选择与随机斜率可能不稳 | 嵌套 GroupKFold、簇 Bootstrap、残差诊断 | 输出技术/生物变异和纵向达标概率 | 主模型 |
| GAMM | 平滑函数 + 孕妇随机效应 | 对孕周/BMI 使用惩罚样条；REML 选平滑度 | 数据支持平滑曲率 | 对非线性更柔性 | 实现成本高，平滑项解释较长 | 有效自由度、外样本曲线与覆盖率 | 若主模型残差仍有曲率则替换 | 显著非线性且样本主体区间足够密集 |
| Beta 混合回归 | 直接对比例分布建模 | Beta 均值链接 + 随机效应 | 响应严格位于 (0,1) | 分布支持匹配 | 本题 Y 远离 1，收益未必抵消复杂度 | PIT/残差、CV 对比 | 仅作分布备用 | logit-LMM 残差严重异方差时 |
| 随机森林 | 非参数回归 | 相同临床特征、固定孕妇折、每孕妇等权 | 只要求局部预测，不要求参数解释 | 可发现遗漏阈值和交互 | 无随机效应解释，尾部外推弱 | 与 LMM 固定效应预测做配对 Bootstrap | 只作非线性审计 | 局部预测显著优于主模型时 |

**推荐模型**：BMI 组间/组内分解的简洁混合效应模型。  
**选用理由**：它直接处理同一孕妇的相关性，可检验题目要求的显著性，还能输出个体差异、误差和纵向概率，为问题二提供真实接口。  
**备选模型**：若主模型残差在孕周或 BMI 上仍呈系统性弯曲，改用 GAMM；若变换后残差和比例边界问题明显，使用 Beta 混合回归。  
**多模型对比建议**：固定外层孕妇折，所有模型回到 Y 原尺度比较 RMSE、MAE、预测区间覆盖率和校准；统计模型另比较 BIC、效应稳定性和解释能力。

## 6.4 创新与改进方向

| 创新或改进方向 | 基础方案 | 具体改动与实现步骤 | 预期改进 | 新增工作量 | 验证指标与对照实验 | 风险及备用方案 | 影响的问题 |
|---|---|---|---|---|---|---|---|
| BMI 组间/组内分解 | 直接使用当前 BMI | 首次 BMI 表示孕妇间差异，\(\Delta BMI\) 表示孕期内变化 | 避免把两种效应混在一起，也防止后续决策泄漏 | 低 | 系数、BIC、外样本误差 | 组内变化太小则删除 \(\Delta BMI\) | Q1—Q3 |
| 单非线性块晋级 | 一次放入多个结点和交互 | 从线性基线出发，每次只增加一个结构块，训练折内选型 | 降低过拟合和事后显著性 | 中 | 配对 CV、整流程 Bootstrap | 无稳定增益则保留线性 | Q1 |
| 纵向—时点双向校验 | Q1 与 Q2 各自独立 | 从 Q1 重构 \(P(Y\ge0.04)\)，与 Q2 的 \(F(t)\) 对照 | 加强跨问闭环 | 中 | 时间点校准差、曲线距离 | 只作校验，不强行融合 | Q1—Q2 |
| 新孕妇预测口径 | 用随机效应拟合值评价 | 测试孕妇随机效应固定为 0，仅用总体效应预测 | 防止不现实的预测乐观 | 低 | 新孕妇 RMSE/覆盖率 | 另报已随访孕妇条件预测 | Q1 |

## 6.5 推荐模型展开

### 6.5.1 响应变换

附件男胎 Y 浓度范围约为 0.0100—0.2342，均在 (0,1) 内，可使用：

\[
Z_{iv}=\operatorname{logit}(\widetilde Y_{iv})
=\log\frac{\widetilde Y_{iv}}{1-\widetilde Y_{iv}}.
\]

无需人为加入 \(10^{-6}\) 修正。若正式数据清洗后出现 0 或 1，再使用边界修正或 Beta 混合模型。

### 6.5.2 BMI 分解

\[
B_{iv}=B_{i0}+\Delta B_{iv}.
\]

- \(B_{i0}\)：首次 BMI，表示孕妇间差异；
- \(\Delta B_{iv}\)：同一孕妇相对首次 BMI 的变化，表示组内效应。

### 6.5.3 基线模型 M0

\[
Z_{iv}
=\beta_0
+\beta_t(t_{iv}-\bar t)
+\beta_B(B_{i0}-\bar B)
+\beta_W\Delta B_{iv}
+u_{0i}+\varepsilon_{iv},
\]

\[
u_{0i}\sim N(0,\sigma_{u0}^2),
\qquad
\varepsilon_{iv}\sim N(0,\sigma^2).
\]

### 6.5.4 非线性候选 M1

只允许一个结构块首先晋级。孕周候选：

\[
f_t(t)
=\beta_1(t-\bar t)
+\beta_2(t-\kappa_t)_+,
\quad
\kappa_t\in\{13,14,15,16,17,18\}.
\]

BMI 候选：

\[
f_B(B)
=\gamma_1(B-\bar B)
+\gamma_2(B-\kappa_B)_+,
\]

其中 \(\kappa_B\) 从首次 BMI 的 20%—80% 分位数候选中选择。结点选择必须在每个训练折内完成。

如果孕周和 BMI 的非线性块都获得稳定支持，再拟合 M2；否则只保留一个。

### 6.5.5 交互与随机斜率候选 M2

候选交互：

\[
\beta_{tB}(t_{iv}-\bar t)(B_{i0}-\bar B).
\]

候选随机斜率：

\[
u_{0i}+u_{1i}(t_{iv}-\bar t),
\qquad
\begin{pmatrix}u_{0i}\\u_{1i}\end{pmatrix}
\sim N(0,\Sigma_u).
\]

只有满足以下条件才保留：

- 无奇异拟合；
- BIC 或外样本误差有稳定改善；
- Bootstrap 中入选频率至少 60%；
- 效应方向在孕周主体区间稳定。

## 6.6 显著性与解释

### 固定效应

- 单系数：Wald 检验与簇 Bootstrap 区间；
- 非线性块：似然比检验 + 整流程 Bootstrap；
- 交互：同时报告效应曲线，不只报 p 值。

### 随机效应

随机截距模型可报告：

\[
ICC=\frac{\sigma_{u0}^2}{\sigma_{u0}^2+\sigma^2}.
\]

若存在随机斜率，同一孕周 \(t\) 的方差贡献为：

\[
V_u(t)
=\sigma_{u0}^2
+2(t-\bar t)\sigma_{01}
+(t-\bar t)^2\sigma_{u1}^2,
\]

\[
ICC(t)=\frac{V_u(t)}{V_u(t)+\sigma^2}.
\]

此时不能继续只报一个固定 ICC。

### 模型解释

最终正文应给：

- 典型 BMI 下的 Y—孕周效应曲线与 95% 区间；
- 典型孕周下的 Y—BMI 曲线；
- 首次 BMI 与 \(\Delta BMI\) 的效应区分；
- 随机效应方差或 \(ICC(t)\)；
- 原尺度预测覆盖率；
- 各模型复杂度与外样本误差。

## 6.7 纵向达标概率与问题二接口

对新孕妇随机效应设为 0，得到总体预测均值 \(\widehat\mu_Z(t,B)\) 和预测方差 \(\widehat V_Z(t,B)\)。近似达标概率为：

\[
\widehat P_{LMM}(Y^*(t)\ge0.04\mid B)
=1-\Phi\left(
\frac{\operatorname{logit}(0.04)-\widehat\mu_Z(t,B)}
{\sqrt{\widehat V_Z(t,B)}}
\right).
\]

该曲线不直接生成问题二最终分组，只用于：

1. 检查达标概率是否随孕周总体上升；
2. 检查 BMI 越高时曲线是否整体右移；
3. 与 AFT 的 \(F_2(t\mid BMI)\) 在 12、14、16、18、20 周对照；
4. 识别 AFT 与纵向模型分歧最大的 BMI 区间。

## 6.8 机器学习非线性审计

特征限定为：孕周、当前 BMI、首次 BMI、年龄、身高、孕次、产次、IVF/IUI。禁止使用 Y 染色体 Z 值作为机械同源预测变量。

每名孕妇总权重为 1：

\[
w_{iv}=\frac1{m_i}.
\]

现有快速试算：

| 模型 | RMSE | MAE | 加权 \(R^2\) |
|---|---:|---:|---:|
| Ridge | 0.03276 | 0.02636 | 0.028 |
| 随机森林 | 0.03232 | 0.02570 | 0.054 |

随机森林相对 Ridge 的 RMSE 仅下降约 1.35%。正式比较必须把推荐混合模型的“新孕妇总体预测”加入同一表。只有随机森林的配对 RMSE 改善 95% 区间完全大于 0，且校准/覆盖不恶化时，才允许在局部区间辅助解释；否则只在附录报告“未发现显著非线性增益”。

## 6.9 问题一最终输出模板

| 变量/效应 | 估计值或效应范围 | 95% 区间 | p 值/块检验 | 临床/统计解释 | 是否传给 Q2 |
|---|---:|---:|---:|---|---|
| 孕周主效应 | 待计算 | 待计算 | 待计算 | 随孕周变化方向 | 是 |
| 首次 BMI | 待计算 | 待计算 | 待计算 | 孕妇间差异 | 是 |
| \(\Delta BMI\) | 待计算 | 待计算 | 待计算 | 孕期内变化 | 否，防止决策泄漏 |
| 孕周×BMI | 待计算 | 待计算 | 待计算 | 高 BMI 是否改变增长速度 | 条件传递 |
| 随机效应/ICC | 待计算 | 待计算 | — | 个体异质性 | 用于校验 |
| \(\sigma_{tech}\) | 约 0.00613 | Bootstrap | — | 技术误差 | 是 |

---

# 7. 问题二：BMI 分组与最佳 NIPT 时点

## 7.1 问题概述

### 任务

仅依据男胎孕妇 BMI 进行合理分组，给出每组 BMI 区间和最佳 NIPT 时点，使潜在风险最小，并分析检测误差对结果的影响。

### 已知与继承

- 继承 Q1 的技术误差 \(\widehat\sigma_{tech}\)；
- 继承 BMI 与达标时点的方向证据；
- 使用首次 BMI，不使用未来 BMI；
- 一名孕妇只构造一个潜在达标事件。

### 输出

- BMI 切点与连续区间；
- 每组孕妇数；
- 推荐时点（十进制周与周+天）；
- 个体可靠度与组覆盖率；
- 推荐时点区间和切点稳定性；
- 测量误差、4% 阈值、AFT 分布和高 BMI 尾部敏感性；
- 若 25 周内无法保证，输出明确拒绝结论。

### 数学本质

这是“测量误差感知的区间删失事件时间估计 + 一维连续最优分段”。核心不是先预测 Y 再机械求交，而是直接建模首次可靠达标时点，并在可靠性约束下选择最早的可执行检测时间。

## 7.2 总体求解思路

1. 将每次访视划分为可信未达标、不确定、可信达标；
2. 构造一名孕妇一个 \((L_i,R_i]\) 区间；
3. 对可信状态冲突者做宽区间/隐状态敏感性；
4. 拟合 BMI-only log-normal AFT，并施加非提前的单调方向；
5. 与 log-logistic、Weibull 和 Q1 纵向概率校验；
6. 定义统一保护水平 \(\rho\)，计算个体稳健可靠时点；
7. 按 BMI 排序，用精确动态规划形成连续分组；
8. 使用上取整次序统计量保证覆盖约束；
9. 切点取整后回代重新计算可靠度；
10. 对误差、阈值、分布、\(\rho\)、组数和尾部支持做敏感性。

## 7.3 可用模型及选型比较

| 可行模型/思路 | 模型本质与核心变量 | 完整实现步骤 | 所需数据与假设 | 优点 | 局限与失败风险 | 验证方法 | 与前后问题的接口 | 适用场景 |
|---|---|---|---|---|---|---|---|---|
| 分位数浓度轨迹求交 | 拟合 Y 的低分位数曲线，与 4% 求交 | 按 BMI 组拟合 \(Q_\tau(Y\mid t,B)\)，求交点 | 分位数曲线可稳定估计 | 直观、贴近浓度解释 | 首次时点仍是间接结果；重复测量需额外处理 | 孕妇 Bootstrap、求交稳定性 | 与 Q1 关系紧密 | AFT 分布严重失配时 |
| **区间删失 AFT + 精确 DP** | 直接估计 \(T_i^{rel}\) 分布并压缩为 BMI 组 | 可信状态；区间似然；个体可靠时点；DP 分组；回代 | AFT 分布在支持域内近似成立 | 直接对应题目输出，可给区间与单调方向 | 重左删失、尾部依赖参数分布 | 三分布、纵向重构、Bootstrap | Q2 主线并冻结为 Q3 基线 | 主模型 |
| 纵向潜在轨迹阈值法 | 混合模型模拟潜在 Y 轨迹的可靠穿越 | 从 Q1 后验抽样轨迹，求首次可靠穿越 | 纵向结构正确 | 能处理阈值回落和误差 | 计算较复杂，受轨迹函数影响 | 与 AFT 时点互校 | Q1→Q2 强接口 | 可信冲突样本较多时 |
| 区间删失 Cox | 半参数风险模型 | 区间删失 PH 拟合，反推出生存分位数 | 比例风险近似成立 | 分布假设较弱 | 推荐孕周分位点不如 AFT 直接 | PH 检验、校准 | Q3 备用 | 三种 AFT 均失配时 |
| 离散时间随机森林 | 在确定状态网格学习 \(P(T\le t)\) | 构造确定行；固定折；时间单调化 | 未知区间行不使用 | 非线性审计 | 信息损失、尾部外推差、非标准删失评分 | 主体区间 Brier/校准 | 只作附录 | 检查 AFT 局部形状 |

**推荐模型**：误差感知区间删失 log-normal AFT + 精确动态规划。  
**选用理由**：它把“一名孕妇一个达标事件”作为统计单位，直接输出孕周分布，并能将可靠度、覆盖率和分组决策连接起来。  
**备选模型**：若 AFT 与纵向重构在主体 BMI 区间持续偏离，优先使用纵向潜在轨迹阈值法；若参数分布均不合适，再使用区间删失 Cox。  
**多模型对比建议**：固定同一删失区间和外层孕妇，比较区间对数损失、时点校准、可靠度覆盖、尾部预测和最终分组决策损失。

## 7.4 创新与改进方向

| 创新或改进方向 | 基础方案 | 具体改动与实现步骤 | 预期改进 | 新增工作量 | 验证指标与对照实验 | 风险及备用方案 | 影响的问题 |
|---|---|---|---|---|---|---|---|
| 首次可靠达标 | 第一次观测阳性 | 技术误差生成三状态；不确定访视扩区间；冲突者单独处理 | 避免阈值噪声伪造事件 | 中 | 回落者数量、区间宽度、AFT 校准 | 硬阈值作基线 | Q1—Q3 |
| 单一政策保护水平 \(\rho\) | 独立调 q 和 alpha | 主文令个体可靠度与组覆盖目标均为 \(\rho\)，其他组合仅敏感性 | 减少任意超参数 | 低 | 风险—时点曲线 | 保留 q—alpha 二维附录 | Q2—Q3 |
| 上取整覆盖 | 默认线性分位数 | 使用第 \(\lceil\rho n\rceil\) 个次序统计量 | 严格满足覆盖约束 | 低 | 回代成员覆盖率 | 无 | Q2—Q3 |
| 精确 DP + 临床取整 | GA 搜索切点 | 全局精确分割；中点切分；取整后回代 | 可复现、有最优性、易落地 | 中 | 目标重算、邻域枚举 | 若切点不稳则合组 | Q2—Q3 |
| 无支撑外推拒绝 | 任何 BMI 都给时点 | 支持域审计；25 周内不达可靠度则拒绝强结论 | 防止伪精确 | 低 | 尾部留出覆盖率 | 给保守区间和复检建议 | Q2—Q3 |

## 7.5 可信状态与删失区间

### 7.5.1 测量误差下的达标概率

对聚合后的访视，定义：

\[
p_{iv}^{obs}
=P(Y_{iv}^{*}\ge c\mid \widetilde Y_{iv}),
\qquad
c=0.04.
\]

在近似正态误差下：

\[
p_{iv}^{obs}
\approx
\Phi\left(
\frac{\widetilde Y_{iv}-c}{SE_{iv}}
\right).
\]

严格地说，该式是在局部平坦先验下的操作性后验/置信分数，不应包装成无需假设的真实后验概率。频率学派下可用等价的一侧置信界定义状态：

\[
\widetilde Y_{iv}
-z_{1-\eta}SE_{iv}\ge c
\quad\Rightarrow\quad
\text{可信达标},
\]

\[
\widetilde Y_{iv}
+z_{1-\eta}SE_{iv}<c
\quad\Rightarrow\quad
\text{可信未达标}.
\]

主文选其中一种表述并保持一致；两种口径的状态应在数值容差内一致。

若采用中位数聚合，\(SE_{iv}\) 不直接机械写成 \(\sigma/\sqrt R\)，而由参数 Bootstrap 模拟同样数量的复测中位数得到；单次检测使用全局技术误差。

主阈值 \(\eta=0.025\)：

\[
S_{iv}=
\begin{cases}
0,&p_{iv}^{obs}\le\eta,\quad\text{可信未达标},\\
1,&p_{iv}^{obs}\ge1-\eta,\quad\text{可信达标},\\
?,&\text{其他，阈值附近不确定}.
\end{cases}
\]

同时对 \(\eta\in\{0.01,0.05,0.10\}\) 做敏感性。

### 7.5.2 区间构造

对每名孕妇按孕周排序：

1. 找到首个可信达标孕周 \(R_i\)；
2. 找到 \(R_i\) 之前最近的可信未达标孕周 \(L_i\)；
3. 中间不确定访视不强制改变状态。

得到：

- 无 \(L_i\)、有 \(R_i\)：左删失 \((0,R_i]\)；
- 有 \(L_i<R_i\)：区间删失 \((L_i,R_i]\)；
- 有可信未达标、无可信达标：右删失 \((L_i,+\infty)\)；
- 全部不确定：无信息区间，主似然不强行使用，进入多重扰动分析。

### 7.5.3 状态冲突

若首个可信达标之后出现可信未达标：

- 主分析仍以“首次可靠达标”记录事件；
- 标记 `state_conflict=1`；
- 敏感性 A：排除这些孕妇重新拟合；
- 敏感性 B：对潜在状态做时间保序/隐状态平滑；
- 若两种敏感性使组时点变化超过 0.5 周，则扩大区间或合并相关 BMI 组。

## 7.6 区间删失 AFT 主模型

### 7.6.1 线性主模型

标准化首次 BMI：

\[
Z_{B,i}=\frac{B_{i0}-\bar B}{s_B}.
\]

建立：

\[
\log T_i^{rel}
=\alpha_0+\alpha_B Z_{B,i}+\sigma\varepsilon_i,
\qquad
\varepsilon_i\sim N(0,1),
\]

并约束：

\[
\alpha_B\ge0.
\]

### 7.6.2 单调分段备用

若主体区间出现稳定非线性：

\[
\eta(B)
=\alpha_0+\alpha_1(B-\bar B)
+\sum_{h=1}^{H}\alpha_{h+1}(B-\kappa_h)_+,
\]

并在观测 BMI 网格上约束：

\[
\eta'(B)\ge0.
\]

结点数量最多 1—2 个；训练折内按 BIC 和校准选择，若与线性模型差异在一个标准误内，保留线性模型。

### 7.6.3 区间似然

对数正态 AFT 的分布函数：

\[
F(t\mid B_i)
=\Phi\left(
\frac{\log t-\eta(B_i)}{\sigma}
\right).
\]

单个孕妇似然贡献：

\[
\ell_i=
\begin{cases}
\log F(R_i\mid B_i),&L_i=0,\\[4pt]
\log\left[F(R_i\mid B_i)-F(L_i\mid B_i)\right],
&0<L_i<R_i<\infty,\\[4pt]
\log\left[1-F(L_i\mid B_i)\right],&R_i=\infty.
\end{cases}
\]

数值实现：

- 以 \(\log\sigma\) 参数化保证 \(\sigma>0\)；
- 区间概率用 log-diff-exp 稳定计算；
- 多起点优化并记录收敛状态；
- 单调约束用 SLSQP 或参数重参数化；
- 最终重算每名孕妇的似然贡献，排查数值下溢。

## 7.7 AFT 快速复算与分布敏感性

硬阈值区间、线性 log-normal AFT 的快速试算为：

\[
\log T_i
=2.04707
+0.11701
\frac{BMI_i-31.84744}{2.98614}
+0.53399\,\varepsilon_i.
\]

三种分布的快速比较：

| 分布 | AIC | BMI=28.5 的 90% 时点 | BMI=30.5 | BMI=36 | BMI=40 |
|---|---:|---:|---:|---:|---:|
| log-normal | 368.03 | 13.47 | 14.56 | 18.07 | 21.13 |
| log-logistic | 368.55 | 13.26 | 14.35 | 17.83 | 20.87 |
| Weibull | 368.49 | 13.82 | 14.87 | 18.20 | 21.08 |

三者差异较小，支持使用 log-normal 作为可解释主模型。但该表未加入误差感知区间和整流程 Bootstrap，不能直接作为最终结果。

## 7.8 个体稳健可靠时点

为减少主文参数数量，定义统一政策保护水平：

\[
\rho\in\{0.80,0.90\}.
\]

个体可靠时点：

\[
\tau_i(\rho)
=\inf\{t:F(t\mid B_{i0})\ge\rho\}.
\]

对数正态线性 AFT 下：

\[
\tau_i(\rho)
=\exp\left[
\eta(B_{i0})
+\sigma\Phi^{-1}(\rho)
\right].
\]

通过孕妇级 Bootstrap 得到 \(F\) 的 95% 下置信界，定义稳健时点：

\[
\tau_i^{rob}(\rho)
=\inf\left\{
t:
\underline F_{0.95}(t\mid B_{i0})\ge\rho
\right\}.
\]

若在 \([10,25]\) 内不存在满足条件的 \(t\)，记为：

\[
\tau_i^{rob}(\rho)=\text{NA：常规窗口内无法保证}.
\]

不得用 25 周截断后冒充可行解。

### 7.8.1 不可保证个体在 DP 中的处理

计算时保留未截断的 \(\tau_i^{rob}\)。若其大于 25 周，另设：

\[
I_i^{out}
=\mathbf1\{\tau_i^{rob}>25\}.
\]

若数值上无法得到有限稳健分位，则令 \(\tau_i^{rob}=+\infty\)。对候选组 \(G\)：

\[
t_G^{raw}
=Q_\rho^\uparrow
\{\tau_i^{rob}:i\in G\},
\]

\[
t_G^{op}
=\max(10,t_G^{raw}).
\]

只有 \(t_G^{op}\le25\) 时该组在常规窗口内可行；否则该候选段在 DP 中记为无穷损失。这样，少于 \(1-\rho\) 的窗口外个体可以被组覆盖约束明确容纳并单列提醒，但不能通过把其时点截成 25 周伪造可行性。

## 7.9 风险定义与组时点

### 7.9.1 词典序决策

按以下优先级决策：

1. 个体达标可靠度至少为 \(\rho\)；
2. 每组至少有 \(\rho\) 比例成员满足该个体可靠度；
3. 在前两项满足时选择最早时点；
4. 外样本性能相近时选择更少组数和更稳定切点；
5. 无法满足时输出不可保证，而不是降低标准不说明。

这样主文只有一个政策参数 \(\rho\)，而不是同时展示大量 q—alpha 组合。

### 7.9.2 严格组时点

将组内稳健个体时点排序：

\[
\tau_{(1)}^{rob}\le\cdots\le\tau_{(|G|)}^{rob}.
\]

组推荐时点：

\[
t_G(\rho)
=\tau_{(\lceil\rho|G|\rceil)}^{rob}.
\]

因此至少有：

\[
\frac1{|G|}
\sum_{i\in G}
\mathbf 1\{\tau_i^{rob}(\rho)\le t_G\}
\ge\rho.
\]

必须使用上取整次序统计量，不能使用默认线性插值分位数。

### 7.9.3 临床时间风险呈现

对最终 \(t_G\) 标注：

- \(t_G\le12\)：早期窗口；
- \(12<t_G\le27\)：中期高风险窗口；
- \(t_G>27\)：晚期极高风险窗口。

优化目标不人为赋予未经题目支持的精确医疗代价，只在满足可靠度后尽量提前，并通过两档 \(\rho\) 展示风险偏好。

## 7.10 BMI 连续分组的精确动态规划

### 7.10.1 排序与约束

\[
B_{(1)}\le B_{(2)}\le\cdots\le B_{(n)}.
\]

约束：

- 每组至少 \(n_{min}=\max(20,\lceil0.07n\rceil)\) 人；
- 相同 BMI 不允许被切到两个组；
- 组数 \(K\in\{2,3,4,5,6\}\)；
- 不强制最小 BMI 宽度，避免人为排除高密度窄组；复杂度由最小样本数和 K 控制。

### 7.10.2 段代表时点与损失

对候选连续段 \(i{:}j\)，令：

\[
t_{ij}^{raw}
=Q_{\rho}^{\uparrow}
(\tau_i^{rob},\ldots,\tau_j^{rob}),
\]

其中 \(Q_{\rho}^{\uparrow}\) 为上取整经验分位数。

\[
t_{ij}=\max(10,t_{ij}^{raw}).
\]

采用分位数损失：

\[
\rho_{\rho}(u)
=u\left[\rho-\mathbf 1(u<0)\right],
\]

\[
C(i,j)
=\sum_{r=i}^{j}
\rho_{\rho}(\tau_r^{rob}-t_{ij}).
\]

该损失衡量把个体可靠时点压缩成同一组时点所造成的决策异质性。

若 \(t_{ij}>25\)，则令：

\[
C(i,j)=+\infty,
\]

表示该段在题目常规检测窗口内无法满足覆盖要求。

### 7.10.3 递推

令 \(DP(k,j)\) 表示前 \(j\) 个排序孕妇分成 \(k\) 组的最小损失：

\[
DP(1,j)=C(1,j),
\]

\[
DP(k,j)
=\min_{i}
\left\{
DP(k-1,i)+C(i+1,j)
\right\},
\]

其中每一段必须满足：

\[
j-i+1\ge n_{min}.
\]

### 7.10.4 伪代码

```text
输入：首次 BMI B[1:n]、稳健个体时点 tau[1:n]、保护水平 rho
排序：按 B 升序；相同 BMI 保持同组约束
候选组数：K = 2,...,6

for every feasible segment i:j:
    rank = ceil(rho * (j-i+1))
    t_raw[i,j] = tau[i:j] 的第 rank 个次序统计量
    t[i,j] = max(10, t_raw[i,j])
    if t[i,j] > 25:
        C[i,j] = infinity
    else:
        C[i,j] = 分位数损失之和

for K in 2,...,6:
    DP[1,j] = C[1,j]
    for k in 2,...,K:
        for j in feasible endpoints:
            DP[k,j] = min_i DP[k-1,i] + C[i+1,j]
            保存最优前驱
    回溯得到切点和组时点

用训练孕妇拟合 AFT 和切点；
把切点应用到外层测试孕妇；
计算测试决策损失、可靠度、覆盖率和尾部风险；
用一标准误规则选择最小 K。
```

复杂度约为 \(O(Kn^2)\)。本题 \(n=267\)，精确求解完全可行，无需遗传算法。

### 7.10.5 切点落地

若最优切分发生在 \(B_{(j)}\) 与 \(B_{(j+1)}\) 之间，原始切点：

\[
c=\frac{B_{(j)}+B_{(j+1)}}2.
\]

落地时取最近 0.5 或整数。取整后必须：

1. 重新分配全部孕妇；
2. 重新计算每组 \(t_G\)；
3. 重新检查 \(\rho\) 覆盖约束；
4. 若违约，延后时点或合并组；
5. 报告取整前后决策损失差。

## 7.11 修正后的快速 DP 试算

以硬阈值区间、线性 log-normal AFT、\(\rho=0.80\)、\(K=4\) 做路线核验，切点与原试算一致，但使用上取整分位数后，时点和覆盖率修正为：

| 快速试算 BMI 区间 | 人数 | 修正推荐周 | 约合周+天 | 满足个体可靠时点的成员比例 |
|---|---:|---:|---:|---:|
| [20.70, 31.03) | 123 | 11.49 | 11周+3天 | 0.813 |
| [31.03, 33.62) | 84 | 12.76 | 12周+5天 | 0.810 |
| [33.62, 36.10) | 40 | 14.03 | 14周+0天 | 0.825 |
| [36.10, 46.88] | 20 | 16.80 | 16周+6天 | 0.850 |

原线性插值分位数使前两组覆盖率只有 0.797 和 0.798，低于 0.80；修正后严格满足约束。该表仍未使用误差感知区间、稳健置信下界、外样本选 K 和临床切点取整，因此只验证算法数量级。

## 7.12 检测误差与不确定性传播

### 方案 A：可信状态重构

按 \(p_{iv}^{obs}\) 将阈值附近记录设为不确定，重新构造删失区间。这是主分析。

### 方案 B：多重扰动

重复 \(M=500\) 或 1000 次：

\[
Y_{iv}^{(m)}
=\operatorname{clip}
(\widetilde Y_{iv}+e_{iv}^{(m)},0,1),
\]

\[
e_{iv}^{(m)}\sim N(0,\kappa^2\widehat\sigma_{tech}^2),
\qquad
\kappa\in\{0.75,1,1.25,1.5\}.
\]

每次重新：

- 判定可信状态；
- 构造区间；
- 拟合 AFT；
- 计算个体时点；
- 动态规划分组；
- 记录切点、时点、分组切换和不可保证人数。

### 方案 C：技术误差与模型残差分离

\(\sigma_{tech}\) 用于观测状态不确定性；Q1 的残差 \(\sigma_{res}\) 表示技术误差、生物波动和模型遗漏的混合，不直接与 \(\sigma_{tech}\) 相加后称为“测量误差标准误”。保守总噪声只作为压力测试，并明确改变了误差含义。

## 7.13 问题二最终输出模板

| BMI 区间 | 样本数 | 支持域状态 | 推荐孕周 | 周+天 | 组覆盖率 | 平均达标概率 | 时点 95% 区间 | 切点稳定性 | 操作建议 |
|---|---:|---|---:|---|---:|---:|---|---:|---|
| 待计算 | 待计算 | 主体/尾部 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 常规/复检/不可保证 |

正文同时给两档：

- \(\rho=0.80\)：平衡方案；
- \(\rho=0.90\)：保守方案。

其余 \(\rho\) 和 q—alpha 解耦结果只放敏感性附录。

---


# 8. 问题三：多因素作用下的 BMI 分组与稳健时点

## 8.1 问题概述

### 任务

在问题二仅使用 BMI 的基础上，综合身高、体重、年龄、孕次、产次、受孕方式等因素，并考虑检测误差和 Y 浓度达标比例，给出男胎孕妇的合理 BMI 分组与最佳 NIPT 时点。

### 已知与继承

- 问题二已经得到误差感知的首次可靠达标区间 \((L_i,R_i]\)；
- 问题二已经建立 BMI-only 区间删失 AFT；
- 问题二已经给出可直接执行的 BMI 连续分组动态规划；
- 技术误差 \(\widehat\sigma_{tech}\)、可靠度 \(\rho\) 和孕妇级验证折已经固定；
- 问题三仍要求“按 BMI 分组”，所以其他变量用于修正个体时点，而不是把最终规则改成难以执行的多维树。

### 输出

问题三必须回答四个层次：

1. BMI 之外的变量是否带来稳定、可重复的增量信息；
2. 若有增量，个体可靠时点如何修正；
3. 修正后是否需要改变问题二的 BMI 切点和组时点；
4. 高 BMI、稀疏组合或检测误差增大时，方案是否仍可靠。

### 数学本质

问题三不是重新发明一套模型，而是对问题二做一个受约束的嵌套升级：

\[
\boxed{
\text{BMI-only 冻结基线}
\longrightarrow
\text{多因素增量检验}
\longrightarrow
\begin{cases}
\text{证据不足：保留 Q2}\\
\text{证据充分：修正个体时点并重跑 DP}
\end{cases}
}
\]

这样既保证四问连贯，也避免“问题二一套结论、问题三全部推翻”的叙事断裂。

## 8.2 总体求解思路

1. 保留问题二的访视键、可信状态、删失区间和验证折；
2. 将 BMI-only AFT 的模型形式、分布和超参数冻结为基线；
3. 构造只含检测前可得信息的多因素候选集；
4. 用嵌套区间删失 AFT 估计变量增量；
5. 在相同孕妇外层折比较区间对数得分、时间校准和决策损失；
6. 用整流程孕妇簇 Bootstrap 检查变量入选率、系数方向、切点和推荐时点；
7. 只有增量模型达到预先规定的晋级门槛，才重新计算个体可靠时点和 BMI 动态规划；
8. 若平均指标相近，用高 BMI 尾部风险和最坏 10% 短缺作为破平指标；
9. 测序失败没有可靠标签时只做情景分析，不凭空拟合“失败概率模型”；
10. 最终同时报告“Q2 基线”和“Q3 修订”，明确改变来自哪些变量。

## 8.3 可用模型及选型比较

| 可行模型/思路 | 模型本质与核心变量 | 完整实现步骤 | 所需数据与假设 | 优点 | 局限与失败风险 | 验证方法 | 与前后问题的接口 | 适用场景 |
|---|---|---|---|---|---|---|---|---|
| **BMI-only 区间删失 AFT** | \(\log T\) 只由首次 BMI 决定 | 直接冻结 Q2 拟合和 DP 输出 | Q2 假设不变 | 最简单、可解释、是公平增量基线 | 可能遗漏年龄、身高等异质性 | 相同外层折的区间得分与校准 | 完整继承 Q2 | 多因素无稳定增益时即为最终模型 |
| **嵌套多因素 AFT** | 在 BMI-only 上逐块加入检测前变量 | 标准化；按临床块加入；训练折选型；外层折评价 | AFT 时间比近似稳定；协变量在检测前可得 | 可直接修正个体时点，叙事最连贯 | 小样本下变量多会不稳；共线性高 | 配对 CV、似然比、BIC、簇 Bootstrap | 修正 \(t_i^{(\rho)}\)，继续使用 Q2 的 DP | 主候选 |
| 惩罚区间删失 AFT | 对多因素系数施加 Ridge/Elastic Net | 训练折选择惩罚；外层折预测；Bootstrap 选择稳定性 | 允许弱效应、多重共线 | 稳定性较好 | 系数推断与区间更复杂 | 嵌套 CV、稳定选择频率 | 可作为多因素 AFT 的稳定化版本 | 候选变量较多且普通 MLE 不稳时 |
| 区间删失 Cox | 不规定基线分布，估计相对风险 | 构造区间数据；估计比例风险；输出 \(F(t\mid x)\) | 比例风险假设近似成立 | 分布假设较弱 | 直接解释时间与求高分位较不直观 | PH 诊断、外层区间得分 | 作为 AFT 分布敏感性 | 三种 AFT 分布分歧明显时 |
| 随机生存森林 | 非线性生存/删失预测 | 相同外层折；限制树深；输出时间分布 | 样本量足以支撑局部结构 | 能发现非线性和交互 | 尾部外推差、解释和稳定性弱 | 配对得分、校准、切点稳定性 | 只申请“存在非线性增量”的证据 | 多因素 AFT 残差仍有稳定结构时 |
| 纵向—生存联合模型 | 同时拟合 Y 轨迹和达标时点 | 建立潜在轨迹、测量误差和越界时间联合似然 | 强分布与轨迹假设 | 理论完整 | 本题工作量过大，易压垮主线 | 与简洁 AFT 的预测对照 | 仅作拓展 | 论文篇幅和计算资源充足时 |

**推荐模型**：嵌套多因素区间删失 AFT；当普通 MLE 不稳定时使用轻度 Ridge 稳定化。  
**冻结基线**：问题二 BMI-only AFT，任何新增模型都必须在同一数据、同一划分、同一指标下比较。  
**备用模型**：区间删失 Cox 只用于检查分布假设；随机生存森林只作非线性审计。  
**不推荐作为主线**：纵向—生存联合模型。它理论上漂亮，但对本题最终 BMI 分组的边际收益不确定，容易形成方法堆砌。

## 8.4 创新与改进方向

| 创新或改进方向 | 基础方案 | 具体改动与实现步骤 | 预期改进 | 新增工作量 | 验证指标与对照实验 | 风险及备用方案 | 影响的问题 |
|---|---|---|---|---|---|---|---|
| 冻结基线后的增量建模 | Q3 重新全量选模 | 固定 Q2 口径，仅允许预先定义的变量块晋级 | 避免后问为了复杂而复杂 | 低 | 配对外层得分、BIC、切点变化 | 无增益即冻结 Q2 | Q2—Q3 |
| 决策前变量审计 | 混入检测后或同源指标 | 对每个变量标记“检测前可得/检测后产生” | 防止信息泄漏，提升外推真实性 | 低 | 特征时间戳审计 | 删除不可得变量 | Q3 |
| 多因素个体化、BMI 一维落地 | 直接建立多维分组 | 多因素模型产生 \(t_i^{(\rho)}\)，再按 BMI 做连续分段压缩 | 兼顾预测准确与临床执行 | 中 | 个体时点误差、分组风险 | 若压缩损失大则报告需个体化 | Q3 |
| 尾部风险破平 | 只看平均损失 | 对最坏 10% 时点短缺计算 CVaR | 防止主体样本掩盖高 BMI 尾部 | 中 | CVaR、不可保证比例 | 输出尾部单列建议 | Q2—Q3 |
| “不升级”也是结论 | 只报告最好点估计 | 预先设晋级门槛，区间跨 0 则保留简单模型 | 控制过拟合，增强论证可信度 | 低 | 改善区间、入选率、校准 | 无 | 全文 |

## 8.5 多因素 AFT 模型

### 8.5.1 主参数化

设首次检测前可获得的协变量为：

\[
x_i=
\left(
B_{i0},H_i,A_i,G_i,P_i,I_i
\right),
\]

其中：

- \(B_{i0}\)：首次 BMI；
- \(H_i\)：身高；
- \(A_i\)：年龄；
- \(G_i\)：怀孕次数；
- \(P_i\)：生产次数；
- \(I_i\)：IVF/IUI 等受孕方式指示。

主模型为：

\[
\log T_i
=\alpha_0+f_B(B_{i0})
+\gamma_H H_i^*
+\gamma_A A_i^*
+\gamma_G G_i^*
+\gamma_P P_i^*
+\gamma_I I_i
+\sigma\varepsilon_i,
\]

其中连续变量在训练折内标准化：

\[
X_i^*=\frac{X_i-\bar X_{train}}{s_{X,train}}.
\]

主分析先令 \(f_B(B)=\beta_B B^*\)。只有 Q2 已证明 BMI 非线性且外层验证稳定时，才沿用 Q2 的单调分段形式，不能在 Q3 重新搜索大量结点。

### 8.5.2 共线性替代参数化

由于：

\[
BMI=\frac{Weight}{Height^2},
\]

不能把 BMI、体重、身高作为三个未经正则化的独立线性项同时解释。比较两个互斥方案：

\[
\mathcal M_{BH}:
\quad BMI+Height+Age+\cdots,
\]

\[
\mathcal M_{WH}:
\quad Weight+Height+Age+\cdots.
\]

最终仍需按 BMI 分组，所以若两者预测相当，优先 \(\mathcal M_{BH}\)。\(\mathcal M_{WH}\) 只用来检查 BMI 参数化是否丢失体型结构。

### 8.5.3 区间似然

沿用问题二的 \((L_i,R_i]\)。对协变量 \(x_i\)，令：

\[
\eta_i=\alpha_0+x_i^\top\beta.
\]

以 log-normal AFT 为例：

\[
F(t\mid x_i)
=\Phi\left(
\frac{\log t-\eta_i}{\sigma}
\right).
\]

总对数似然仍为：

\[
\ell(\theta)
=\sum_i\log
\left[
F(R_i\mid x_i)-F(L_i\mid x_i)
\right],
\]

并分别处理左删失和右删失。所有变量选择都必须在训练折内重新完成，测试折只计算冻结模型的区间预测。

## 8.6 变量块与晋级顺序

为减少事后筛选，采用预定义的层级结构：

\[
\mathcal M_0:
\quad BMI,
\]

\[
\mathcal M_1:
\quad BMI+Age+Height,
\]

\[
\mathcal M_2:
\quad \mathcal M_1+Gravidity+Parity+Conception,
\]

\[
\mathcal M_3:
\quad \mathcal M_2+
\text{一个预先指定的非线性块或交互}.
\]

交互最多从以下两项中选择一项：

\[
BMI\times Age,
\qquad
BMI\times Height.
\]

保留层级原则：若交互存在，两个主效应必须存在。

### 8.6.1 单个变量的保留条件

一个新增变量或变量块只有同时满足多数证据才保留：

1. 训练折 BIC 改善；
2. 外层区间对数得分改善；
3. 在 12、14、16、18、20 周至少不恶化校准；
4. 系数方向在至少 80% Bootstrap 中一致；
5. 入选频率至少 60%；
6. 由它引起的切点变化在 Bootstrap 中可重复；
7. 其信息在推荐时点前确实可获得。

### 8.6.2 快速路线核验

附件的快速试算显示，BMI-only log-normal AFT 的 AIC 约为 368.0，而未经充分正则和外层验证的多因素版本 AIC 约为 373.1。该结果提示：

\[
\text{“变量更多”}
\not\Rightarrow
\text{“时点估计更好”}.
\]

因此，多因素模型不得预设为最终优胜者。正式结论必须以嵌套验证和 Bootstrap 为准。

## 8.7 多因素增量的公平验证

### 8.7.1 外层划分

- 以孕妇代码为分组单位；
- 固定 5 折，重复 20 次；
- 同一孕妇的全部访视、删失区间和标签只能进入同一折；
- 预处理、标准化、变量选择、惩罚强度和分布选择都在训练折内完成。

### 8.7.2 主指标：区间对数得分

对测试孕妇的真实删失区间 \((L_i,R_i]\)，预测概率为：

\[
\widehat p_i
=\widehat F(R_i\mid x_i)
-\widehat F(L_i\mid x_i).
\]

外样本负对数得分：

\[
\operatorname{NLL}
=-\frac1{n_{test}}
\sum_i\log(\max(\widehat p_i,\epsilon)).
\]

它直接评价模型给真实区间分配了多少概率，是多因素 AFT 的首要晋级指标。

### 8.7.3 时间点校准

在：

\[
\mathcal T=\{12,14,16,18,20\}
\]

周评估预测达标概率。可用区间删失 NPMLE 或训练折外伪观测构造校准参照，输出：

- 校准截距和斜率；
- 分箱观测概率—预测概率图；
- 集成 Brier 分数；
- 主体 BMI 与高 BMI 尾部分层结果。

### 8.7.4 决策指标

最终模型还必须比较：

1. BMI 切点的 Bootstrap 标准差；
2. 推荐周的绝对变化；
3. 严格组覆盖率是否满足 \(\rho\)；
4. 平均延迟损失；
5. 未覆盖成员的时点短缺；
6. 高 BMI 尾部不可保证比例。

仅改善似然、却使切点和时点大幅摆动的模型，不晋级。

### 8.7.5 配对不确定性

对每个外层折、每名测试孕妇保存 BMI-only 和多因素模型的预测。按孕妇簇做配对 Bootstrap，计算：

\[
\Delta_{NLL}
=NLL_{BMI}-NLL_{multi},
\]

\[
\Delta_{IBS}
=IBS_{BMI}-IBS_{multi}.
\]

正值表示多因素模型改善。晋级要求主指标改善的 95% 区间下界大于 0，且校准、覆盖和尾部风险无实质恶化。

若改善很小但区间下界略大于 0，还应要求达到预设最小实际改善，例如：

\[
\frac{\Delta NLL}{NLL_{BMI}}\ge1\%
\quad\text{或}\quad
\Delta IBS\ge0.002.
\]

阈值应在查看最终测试结果前写入配置文件。

## 8.8 从多因素 AFT 到达标比例

对孕妇 \(i\)，在时间 \(t\) 前可靠达标的预测概率为：

\[
\widehat p_i(t)
=\widehat F(t\mid x_i).
\]

给定 BMI 组 \(G\)，平均达标概率：

\[
\bar p_G(t)
=\frac1{n_G}\sum_{i\in G}\widehat p_i(t).
\]

满足个体可靠度 \(\rho\) 的预测成员比例：

\[
C_G(t;\rho)
=\frac1{n_G}
\sum_{i\in G}
\mathbf1\{\widehat p_i(t)\ge\rho\}.
\]

问题三延续问题二的严格口径：

\[
t_G
=\inf\{t:C_G(t;\rho)\ge\rho\}.
\]

同时报告 \(\bar p_G(t_G)\)，但平均概率不能替代成员覆盖约束。例如少数高概率孕妇不能抵消另一批低概率孕妇。

## 8.9 个体时点修正与 BMI 压缩

多因素模型中，个体可靠时点为：

\[
t_i^{(\rho)}
=\inf\{t:\widehat F(t\mid x_i)\ge\rho\}.
\]

对 log-normal AFT：

\[
t_i^{(\rho)}
=\exp\left(
\widehat\eta_i
+\widehat\sigma z_\rho
\right).
\]

稳健版本沿用 Q2 的置信上界或 Bootstrap 上分位：

\[
\widetilde t_i^{(\rho)}
=Q_{1-\alpha}
\left(
t_i^{(\rho,1)},\ldots,t_i^{(\rho,M)}
\right).
\]

然后仍按首次 BMI 排序，将 \(\widetilde t_i^{(\rho)}\) 送入问题二同一个精确动态规划。这样：

- 多因素模型负责更准确地估计每名孕妇的需求；
- BMI 动态规划负责把个体需求压缩成可执行规则；
- Q2 与 Q3 的差别只来自个体时点输入，算法和风险定义保持一致。

## 8.10 是否重分组的决策门

### 情形 A：多因素模型未晋级

若配对增量区间覆盖 0，或校准/稳定性恶化：

\[
\boxed{
\text{保留问题二的 BMI 切点和组时点}
}
\]

其他因素只作为分层提醒。例如“年龄偏高且高 BMI 的孕妇建议在组时点后复核”，不得伪装成一个已证实的新模型。

### 情形 B：多因素模型晋级但分组变化很小

若个体预测改善，但最优切点在取整后与 Q2 相同，则：

- 保留原切点；
- 只更新部分组的推荐周；
- 将“结构稳定、时间校正”作为结果。

### 情形 C：多因素模型晋级且切点稳定改变

只有新切点在 Bootstrap 中形成清晰模态、取整后仍满足覆盖约束，才采用 Q3 新分组。正文列出：

\[
\Delta c_k=c_k^{Q3}-c_k^{Q2},
\qquad
\Delta t_k=t_k^{Q3}-t_k^{Q2}.
\]

这使评委能够看清问题三到底改变了什么。

## 8.11 高 BMI 尾部与 CVaR 破平

设某方案在组时点 \(t_G\) 下的个体短缺为：

\[
s_i
=\left[
\widetilde t_i^{(\rho)}-t_G
\right]_+.
\]

平均短缺：

\[
\overline s_G
=\frac1{n_G}\sum_{i\in G}s_i.
\]

对最坏 \(1-\eta\) 比例计算条件风险价值：

\[
CVaR_\eta(s)
=\min_{\xi}
\left\{
\xi+\frac1{(1-\eta)n}
\sum_i(s_i-\xi)_+
\right\},
\]

建议 \(\eta=0.90\)。

使用顺序：

1. 先满足最小样本量与严格覆盖率；
2. 再比较主决策损失；
3. 若两个方案主损失相差不超过 1%，选择 \(CVaR_{0.90}\) 更小者；
4. 若最高 BMI 组样本过少，输出“尾部个体化”，不为追求形式完整而继续细分。

附件中：

- BMI \(\ge36\) 仅 20 名孕妇；
- BMI \(\ge40\) 仅 4 名；
- BMI \(\ge42\) 仅 2 名。

因此，40 以上不能写成稳定独立组。更合理的做法是把它们并入最高 BMI 组，同时单列外推警告和保守复检建议。

## 8.12 检测失败仅作情景分析

附件没有可信、独立的“首次采血后实验室检测失败”监督标签。若把低 Y、低读段或晚期复检自行定义成失败，再训练分类器，会形成循环定义。

因此主模型不拟合假想失败概率，只测试：

\[
\pi_f\in\{0,0.05,0.10,0.15\},
\]

或失败后延迟：

\[
\Delta_f\in\{1,2\}\text{ 周}.
\]

一种简单情景是：

\[
P(\text{在 }t\text{ 前获得可用达标结果})
=(1-\pi_f)F_T(t\mid x).
\]

若失败后需要 \(\Delta_f\) 周复检，可用：

\[
P_{usable}(t)
=(1-\pi_f)F_T(t)
+\pi_f F_T(t-\Delta_f).
\]

重新计算各组时点，报告：

- 推荐周推迟多少；
- 哪些组首次违反覆盖约束；
- 高 BMI 尾部是否转为“无法保证”。

情景结果用于稳健性讨论，不作为有数据证据的预测模型。

## 8.13 机器学习增量审计

原方案快速试算中，多因素加入前后的外样本 Brier 结果约为：

| 审计模型 | BMI-only | 加入多因素 | 改善方向 | 初步判断 |
|---|---:|---:|---:|---|
| Logistic | 0.0574 | 0.0631 | 恶化 0.0057 | 不支持升级 |
| Random Forest | 0.0578 | 0.0551 | 改善 0.0027 | 有微弱候选信号 |
| XGBoost | 0.0557 | 0.0556 | 改善 0.0001 | 无实际增益 |

随机森林改善的配对 Bootstrap 区间约为：

\[
0.00270,
\qquad
95\%CI=[-0.00081,0.00667].
\]

区间覆盖 0，不能声称“机器学习显著提升”。本终稿因此：

- 删除 XGBoost 主线；
- 随机森林只做非线性审计；
- 不用机器学习结果直接生成 BMI 切点；
- 若完整重算后改善区间仍覆盖 0，正文只保留一句“未发现稳定增量”；
- 若未来独立数据确认增益，才允许用森林预测的个体时点作为 DP 输入，并继续接受同样的稳定性审查。

## 8.14 问题三最终输出模板

### 模型增量表

| 模型 | 外层 NLL | 集成 Brier | 12—20 周校准 | 参数/自由度 | 高 BMI 尾部风险 | 改善 95% 区间 | 是否晋级 |
|---|---:|---:|---|---:|---:|---|---|
| BMI-only AFT | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 基线 | 是 |
| 多因素 AFT | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待判 |
| 区间 Cox | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 敏感性 |
| 随机生存森林 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 审计 |

### 最终分组表

| BMI 区间 | 样本数 | Q2 推荐周 | Q3 推荐周 | 变化 | 严格覆盖率 | 平均达标概率 | CVaR | 支持域状态 | 最终建议 |
|---|---:|---:|---:|---:|---:|---:|---:|---|---|
| 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 主体/尾部 | 保留/修订/个体化 |

## 8.15 问题三的叙事结论写法

建议论文正文采用下述逻辑：

> 问题二在只允许使用 BMI 的条件下建立了可解释、可执行的基础规则。问题三并未另起炉灶，而是保持可靠达标定义和分组优化器不变，仅检验检测前多因素能否更准确地修正个体时点。当增量模型未获得稳定外样本优势时，保留问题二规则本身就是稳健结论；若增量得到支持，则只更新有证据的组时点或切点。由此，模型复杂度随证据而增加，而不是随题号机械增加。

---

# 9. 问题四：女胎染色体异常的质量感知判定

## 9.1 问题概述

### 任务

以女胎数据中 AB 列给出的异常判定结果为目标，综合 13、18、21、X 染色体 Z 值、X 染色体浓度、GC 含量、测序读段、比对率、重复率、BMI 和年龄等指标，建立女胎异常判定方法。

### 数据特点

附件复核得到：

| 项目 | 记录数 | 孕妇数 |
|---|---:|---:|
| 全部女胎 | 605 | 147 |
| 任一 AB 异常 | 67 | 44 |
| T13 | 23 | 18 |
| T18 | 46 | 30 |
| T21 | 13 | 12 |

同时：

- 43 名孕妇的 AB 状态在其多次记录中发生过变化；
- 总读段与唯一比对读段的 Spearman 相关约为 0.992；
- “胎儿是否健康”列在女胎样本中全部为“是”，没有监督信息；
- 单以 0.4—0.6 作为 GC 硬阈值会标记约 220 条记录，不能直接把这些记录全部删除；
- T13、T18、T21 可以同时出现，属于多标签而非互斥多分类。

### 输出

对每条待判记录输出：

1. 样本质量状态；
2. T13、T18、T21 各自的校准概率与不确定区间；
3. 正常、建议复检或异常；
4. 若异常，给出具体染色体，可同时给出多个；
5. 触发结论的主要证据和适用性警告。

### 数学本质

问题四是：

\[
\boxed{
\text{重复孕妇下的小阳性多标签分类}
+\text{测序质量门控}
+\text{不确定时允许拒判}
}
\]

它与男胎分支的共同决策哲学是“先判断结果是否可靠，再做最早或最明确的判定”。

## 9.2 总体求解思路

1. 从 AB 字符串拆出 T13、T18、T21 三个二元标签；
2. 不把同一孕妇的不同记录拆到训练折和测试折；
3. 删除无信息变量和明显泄漏变量；
4. 先建立与标签独立的非冗余质量向量；
5. 对 T13、T18、T21 分别建立小样本友好的正则逻辑/Firth 模型；
6. 在训练折内完成插补、标准化、特征选择、惩罚选择和概率校准；
7. 以 Z 阈值规则作为必须击败的简单基线；
8. 随机森林仅对 T13/T18 申请局部晋级，XGBoost 不进入最终路线；
9. 用孕妇级重复交叉验证比较 PR-AUC、Brier、敏感度和特异度；
10. 用孕妇簇 Bootstrap 得到概率区间；
11. 质量不足或概率区间跨过决策阈值时输出“建议复检”；
12. 对模型分歧样本不做无依据平均，而是进入复检队列。

## 9.3 可用模型及选型比较

| 可行模型/思路 | 模型本质与核心变量 | 完整实现步骤 | 所需数据与假设 | 优点 | 局限与失败风险 | 验证方法 | 与前后问题的接口 | 适用场景 |
|---|---|---|---|---|---|---|---|---|
| Z 值固定阈值 | 以 \(Z_k\ge3\) 或 \(\lvert Z_k\rvert\ge3\) 判定 | 分染色体应用阈值；低质量样本复检 | Z 值已近似标准化 | 极简、可解释、适合做基线 | 忽略质量和人群偏移；阈值附近不稳 | 孕妇级敏感度、特异度、PR-AUC | 给复杂模型设置最低门槛 | 必须保留的规则基线 |
| **正则逻辑回归** | 线性/单折线 Z 效应 + 临床与质量协变量 | 缺失处理；训练折标准化；Elastic Net；外层校准 | 对数优势结构可近似 | 可解释、稳定、输出概率 | 小阳性下可能分离；复杂非线性有限 | 重复 GroupKFold、Brier、PR-AUC | 主统计分类器 | T13、T18 主候选 |
| **Firth 逻辑回归** | Jeffreys 偏差修正的低维逻辑模型 | 限制变量数；最大化修正似然；Bootstrap 概率 | 小样本、稀有事件 | 缓解完全分离，适合 T21 | 高维特征不可直接堆入 | 簇 Bootstrap、校准、敏感度 | T21 稳健主模型/其他染色体敏感性 | 阳性孕妇很少时 |
| 随机森林 | 局部阈值与非线性交互 | 限深、限制叶节点、类权重、固定外层折 | 数据支持重复抽样下的结构 | T13 快速试算有潜在提升 | 预测区间和外推弱，小阳性方差大 | 配对 PR-AUC/Brier 区间、校准 | 只申请单染色体替换，不做全局堆叠 | T13 或 T18 证据明确时 |
| XGBoost | 梯度提升树 | 调参、早停、概率校准 | 需要较多阳性支撑 | 拟合能力强 | 本题快速试算无稳定优势，复杂度高 | 同外层折比较 | 删除主线，仅留审计记录 | 不推荐 |
| 分类器链/多任务模型 | 用标签相关性联合建模 | 设标签顺序或共享表示 | 多标签相关稳定且样本足够 | 可利用染色体共现 | 标签非常少，链顺序引入不稳 | 嵌套 CV 与独立模型对照 | 仅作未来扩展 | 更大外部数据集 |

**推荐模型族**：三个染色体特异的正则逻辑/Firth 模型。  
**简单基线**：Z 值阈值规则。  
**机器学习候选**：随机森林只对 T13/T18 逐项申请晋级；不得因为 T13 有潜力就把三条染色体全部换成树模型。  
**明确删除**：XGBoost 主模型、分类器链主模型、无验证的加权集成。

## 9.4 创新与改进方向

| 创新或改进方向 | 基础方案 | 具体改动与实现步骤 | 预期改进 | 新增工作量 | 验证指标与对照实验 | 风险及备用方案 | 影响的问题 |
|---|---|---|---|---|---|---|---|
| 质量与异常解耦 | 把质量变量与 Z 值直接堆进分类器 | 先做独立质量门控，再在可判样本上估计异常概率 | 防止低质量噪声被解释成异常 | 中 | 低质量组误报率、复检率 | 门控过严则做覆盖—风险曲线 | Q4 |
| 非冗余质量向量 | 同时使用所有读段派生量 | 总读段与唯一读段择一，GC 用偏离量，稳健标准化 | 降低共线和伪重要性 | 低 | VIF、相关矩阵、校准 | Ridge 保留弱共线 | Q4 |
| 三标签独立建模 | 将所有异常合成一个标签 | 分别估计 T13/T18/T21，可多标签输出 | 不丢失异常类型，适合不同信号强度 | 中 | 分标签 PR-AUC/Brier | 样本太少时扩大复检区，不退回失效的单 Z 硬判 | Q4 |
| 概率区间拒判 | 只按点概率二分类 | Bootstrap 预测区间；跨阈值或质量不足则复检 | 降低不可靠强判定 | 中 | 选择性风险—覆盖曲线 | 区间过宽则只保留质量拒判 | Q4 |
| 模型分歧复检 | 直接平均统计与 ML 概率 | 若两模型各自合格但结论冲突，触发复检 | 把不稳定性显式转为操作建议 | 低 | 分歧率、分歧样本阳性率 | 只保留主模型也可 | Q4 |

## 9.5 标签构造与统计单位

### 9.5.1 三个二元标签

对记录 \(r\)：

\[
y_{r,13}
=\mathbf1\{\text{AB 包含 T13}\},
\]

\[
y_{r,18}
=\mathbf1\{\text{AB 包含 T18}\},
\]

\[
y_{r,21}
=\mathbf1\{\text{AB 包含 T21}\}.
\]

AB 为空时三者均为 0。若同一 AB 同时包含多个异常，多个标签同时为 1，不强制互斥。

### 9.5.2 为什么不先压成孕妇级单标签

43 名孕妇的 AB 状态随记录变化。若直接使用“任一次阳性即终身阳性”，会把早期不确定结果、复检变化和记录级判定混为一谈；若只用最后一次，则会丢失题目指定的记录级 AB。

主分析因此：

- 预测单位：检测记录；
- 数据划分单位：孕妇；
- 权重单位：孕妇总权重为 1；
- 敏感性：另做“首次记录”“最后记录”“任一次阳性”三种孕妇级口径。

若题目附加说明确认 AB 为最终孕妇诊断，再将主分析切换到最终孕妇级标签；在现有附件下不擅自作此假设。

### 9.5.3 重复记录权重

设孕妇 \(i\) 有 \(m_i\) 条记录：

\[
w_{ir}=\frac1{m_i}.
\]

对于类权重，先保证每名孕妇总权重相等，再在训练折内按阳性/阴性有效权重设置：

\[
w_{ir,k}^{class}
=w_{ir}\times
\begin{cases}
\omega_{k,+},&y_{ir,k}=1,\\
\omega_{k,-},&y_{ir,k}=0.
\end{cases}
\]

类权重只能由训练折计算，不能读取测试折阳性率。

## 9.6 非冗余测序质量门控

### 9.6.1 质量变量

建议质量向量为：

\[
q_r=
\left[
\log(U_r+1),
M_r,
D_r,
F_r,
|GC_r-0.5|,
|GC_{13,r}-\widetilde{GC}_{13}|,
|GC_{18,r}-\widetilde{GC}_{18}|,
|GC_{21,r}-\widetilde{GC}_{21}|
\right],
\]

其中：

- \(U_r\)：唯一比对读段数；
- \(M_r\)：比对率；
- \(D_r\)：重复读段比例；
- \(F_r\)：被过滤读段比例；
- \(GC_r\)：总体 GC；
- \(\widetilde{GC}_{k}\)：训练折中相应染色体 GC 的稳健中心。

总读段与唯一比对读段相关约 0.992，主质量向量只保留唯一比对读段。若原始字段定义显示 \(U\) 已由其他比例机械计算，则进一步删去最冗余的派生项。

### 9.6.2 稳健标准化

在训练折内：

\[
z_{rj}^{Q}
=\frac{
q_{rj}-\operatorname{median}(q_{\cdot j})
}{
1.4826\,MAD(q_{\cdot j})+\epsilon
}.
\]

对“越大越差”的变量保留正向，对“越小越差”的变量取负向，统一使较大的 \(z^Q\) 表示质量风险。

### 9.6.3 多变量质量距离

用最小协方差行列式或收缩稳健协方差估计：

\[
D_{Q,r}^2
=(z_r^Q-\widehat\mu_Q)^\top
\widehat\Sigma_Q^{-1}
(z_r^Q-\widehat\mu_Q).
\]

质量标记建议为：

- 单项警告：任一 \(|z_{rj}^Q|>3\)；
- 多项异常：至少两个单项警告；
- 联合异常：\(D_{Q,r}^2\) 超过训练折 99% 分位；
- 极端异常：某关键读段或比对指标超出训练支持域。

不能把 GC 不在 0.4—0.6 的约 220 条记录全部硬删。总体 GC 只作为一个质量信号；只有极端偏离、多个指标同时异常或落入训练外支持域时，才触发强制复检。

### 9.6.4 门控输出

定义：

\[
Q_r\in
\{\text{可判},\text{谨慎可判},\text{建议复检}\}.
\]

可采用：

\[
Q_r=
\begin{cases}
\text{建议复检},
&D_{Q,r}^2>q_{0.99}
\text{ 或至少两个严重标记},\\
\text{谨慎可判},
&D_{Q,r}^2\in(q_{0.95},q_{0.99}]
\text{ 或一个严重标记},\\
\text{可判},&\text{其余}.
\end{cases}
\]

分位点必须在每个训练折内估计。若能获得实验室官方最低读段、比对率等规范，官方阈值优先于经验分位。

## 9.7 特征集合与泄漏审计

### 9.7.1 允许进入分类器

对染色体 \(k\in\{13,18,21\}\)，候选特征分为：

**直接染色体证据**

\[
Z_k,\quad (Z_k-3)_+,\quad GC_k.
\]

**交叉染色体与 X 证据**

\[
Z_{k'},\quad Z_X,\quad X\text{ 浓度}.
\]

交叉染色体 Z 值只在正则化下进入，并检查是否提高校准；不得用它们解释成目标染色体的直接生物机制。

**测序质量**

\[
D_Q,\quad \log U,\quad M,\quad D,\quad F,\quad GC\text{ 偏离}.
\]

**检测前临床变量**

\[
GA,\quad BMI,\quad Age,\quad Height,\quad Gravidity,\quad Parity,\quad Conception.
\]

### 9.7.2 删除或限制

- AB 本身及由 AB 派生的任何字段：绝对禁止；
- “胎儿是否健康”：全为“是”，删除；
- Y 染色体相关空列：结构性缺失，删除；
- 总读段与唯一比对读段：主模型只留一个；
- BMI、体重、身高：正则模型可同时候选，但解释时不能把三个系数当作独立因果效应；
- 检测后才能得到的复检结论：不得用于首次记录判定；
- 孕妇代码：只用于分组和权重，不作为特征。

## 9.8 三个染色体特异模型

### 9.8.1 T13 与 T18：正则逻辑主模型

对 \(k=13,18\)：

\[
\operatorname{logit}
P(y_{r,k}=1\mid x_r)
=\alpha_k+x_r^\top\beta_k.
\]

Elastic Net 目标函数：

\[
\min_{\alpha_k,\beta_k}
\left\{
-\frac1{n}\sum_r
w_{r,k}^{class}
\left[
y_{r,k}\log p_{r,k}
+(1-y_{r,k})\log(1-p_{r,k})
\right]
+\lambda_k
\left[
\frac{1-\gamma_k}{2}\|\beta_k\|_2^2
+\gamma_k\|\beta_k\|_1
\right]
\right\}.
\]

在内层孕妇折选择：

\[
\lambda_k,
\qquad
\gamma_k\in\{0,0.25,0.5,0.75,1\}.
\]

首选指标为加权 Brier 或对数损失，PR-AUC 作第二指标。不能只优化 ROC-AUC。

### 9.8.2 T21：低维 Firth 模型

T21 只有 13 条阳性记录、12 名阳性孕妇。主模型限制为最多 4—6 个预先选定的自由度，例如：

\[
\operatorname{logit}p_{r,21}
=\alpha
+\beta_1Z_{21}
+\beta_2(Z_{21}-3)_+
+\beta_3Z_X
+\beta_4D_Q
+\beta_5Age^*.
\]

Firth 修正似然：

\[
\ell_F(\theta)
=\ell(\theta)+\frac12\log|I(\theta)|.
\]

它能缓解小样本完全分离和系数无穷大。若某折阳性孕妇过少导致模型仍不可识别，则不退回在附件上灵敏度为 0 的单一 Z21 硬规则，而是：

\[
\boxed{
\text{保留质量门控和 Z21 临床参照，扩大复检区，不输出数据驱动的强异常判定}
}
\]

若所有重复划分均无法形成合格校准，则 T21 的诚实结论是“当前附件不足以训练可靠自动判定器”，而不是强行给出一个表面完整的分类模型。

### 9.8.3 不强制三条染色体使用同一算法

每条染色体的阳性数量、特征信号和校准难度不同。最合理的最终组合可以是：

- T13：若证据充分，随机森林；否则正则逻辑；
- T18：正则逻辑；
- T21：低维 Firth；若模型不可识别，则扩大复检区而非退回单 Z 硬判。

这种“分任务晋级”比把一个复杂算法横扫三类更符合数据证据。

## 9.9 概率校准

分类器输出的分数必须变成可解释概率。外层训练集内部再划分校准折：

1. 基础模型在子训练折拟合；
2. 对校准折产生未见样本预测；
3. 使用带轻度收缩的 logistic calibration：

\[
\operatorname{logit}
P(y=1)
=a_k+b_k\operatorname{logit}(\widehat p_{raw});
\]

4. 将基础模型在整个外层训练折重拟合；
5. 用固定的 \(a_k,b_k\) 校准外层测试预测。

小阳性下不优先使用自由度高的 isotonic regression。报告：

- 校准截距 \(a_k\)；
- 校准斜率 \(b_k\)；
- Brier 分数；
- 可靠性图；
- 分层预测概率与实际阳性率。

实际实现优先使用内层折的全部 out-of-fold 预测做交叉拟合校准，而不是再牺牲一块固定校准集。对 T21，若校准样本不足以同时稳定估计截距和斜率，则采用：

\[
b_{21}=1
\]

的仅截距再校准，或对 \(a_{21},b_{21}\) 施加强收缩；若重复验证中仍不稳定，则扩大复检区，不能在测试折上手调阈值补救。

## 9.10 阈值选择

### 9.10.1 基于代价

设漏报、误报、复检代价分别为：

\[
C_{FN,k}>C_{FP,k}>C_R.
\]

若只做二分类，理论阈值近似：

\[
\tau_k
=\frac{C_{FP,k}}
{C_{FP,k}+C_{FN,k}}.
\]

更稳妥的做法是在内层验证中枚举阈值，选择满足：

\[
Sensitivity_k\ge s_{min}
\]

的阈值中，特异度或净收益最高者。建议主文给出一个平衡阈值和一个保守阈值，而不伪造唯一临床代价。

### 9.10.2 不用测试折选阈值

阈值、类权重、校准器和质量门控阈值都必须在外层训练数据内确定。测试折只用于一次无偏评价。

## 9.11 Bootstrap 概率区间与三段式判定

对训练孕妇做 \(M=500\) 或 1000 次簇 Bootstrap。每次完整重拟合预处理、质量中心、分类器和校准器，对待判记录产生：

\[
\widehat p_{r,k}^{(1)},
\ldots,
\widehat p_{r,k}^{(M)}.
\]

定义：

\[
L_{r,k}=Q_{0.025}(\widehat p_{r,k}^{(m)}),
\qquad
U_{r,k}=Q_{0.975}(\widehat p_{r,k}^{(m)}).
\]

对每个染色体：

\[
d_{r,k}=
\begin{cases}
\text{异常},&L_{r,k}\ge\tau_k,\\
\text{正常},&U_{r,k}<\tau_k,\\
\text{建议复检},&L_{r,k}<\tau_k\le U_{r,k}.
\end{cases}
\]

总判定：

\[
D_r=
\begin{cases}
\text{建议复检},&Q_r=\text{建议复检},\\
\text{异常},&\exists k:d_{r,k}=\text{异常},\\
\text{建议复检},&\nexists k:d_{r,k}=\text{异常}
\text{ 且 }\exists k:d_{r,k}=\text{建议复检},\\
\text{正常},&\forall k:d_{r,k}=\text{正常}.
\end{cases}
\]

异常输出可为 \(\{\mathrm{T13},\mathrm{T18}\}\) 等多标签集合。

## 9.12 选择性预测的评价

因为模型允许拒判，除普通分类指标外还要报告：

\[
Coverage
=\frac{\#\{\text{给出正常或异常}\}}{n},
\]

\[
Selective\ Risk
=\frac{\#\{\text{已判样本中的错误}\}}
{\#\{\text{给出正常或异常}\}}.
\]

绘制“覆盖率—选择性风险”曲线。理想方案是随着复检比例增加，已判样本的错误率明显下降。

还应报告：

- 异常样本中的复检率；
- 正常样本中的复检率；
- 低质量组和高质量组的误报率；
- 各染色体的敏感度、特异度、PR-AUC、ROC-AUC、Brier；
- 每 100 名孕妇的预计复检数、漏报数和误报数。

## 9.13 孕妇级重复验证

### 外层验证

- 先在孕妇级表上记录三个标签的“是否曾阳性”；
- T13/T18 使用 5 折 StratifiedGroupKFold，重复 20 次；
- T21 使用 3 折孕妇分组分层验证，重复 20 次，保证每折含阳性孕妇；
- 若现有实现支持多标签迭代分层，可生成兼顾三个标签的主折；否则保存标签特异的折列；
- 对同一个标签，Z 基线、统计模型和随机森林必须使用完全相同的测试孕妇；
- 不把不同标签使用的 5 折与 3 折 AUC 直接作算法优劣比较。

### 内层训练

每个外层训练折内部完成：

- 缺失插补；
- 稳健标准化；
- 质量门控中心与阈值；
- 特征筛选；
- Elastic Net 惩罚；
- 类权重；
- 随机森林超参数；
- 概率校准；
- 决策阈值。

### 配对比较

每条测试记录必须同时得到：

- Z 阈值基线；
- 统计主模型；
- 机器学习候选。

按孕妇簇做配对 Bootstrap。随机森林晋级要求：

1. PR-AUC 改善区间下界大于 0；
2. Brier 改善区间下界大于 0；
3. 预设敏感度下特异度不下降；
4. 校准斜率和截距不恶化；
5. 在至少 80% 重复划分中方向一致；
6. 高 BMI 和低质量亚组没有明显安全性退化。

## 9.14 机器学习快速试算与取舍

原方案快速试算结果为：

| 标签 | 模型 | ROC-AUC | PR-AUC | Brier | 初步解释 |
|---|---|---:|---:|---:|---|
| T13 | Elastic Net | 0.622 | 0.133 | 0.0412 | 统计基线较弱 |
| T13 | Random Forest | **0.819** | **0.214** | **0.0366** | 三项均改善，值得正式复核 |
| T13 | XGBoost | 0.725 | 0.120 | 0.0393 | PR-AUC 不优于基线 |
| T18 | Elastic Net | 0.783 | **0.407** | **0.0587** | 综合最好 |
| T18 | Random Forest | **0.811** | 0.365 | 0.0612 | ROC 提高但 PR/Brier 变差 |
| T18 | XGBoost | 0.805 | 0.290 | 0.0655 | 综合退化 |
| T21 | Elastic Net | **0.617** | **0.110** | **0.0222** | 三者中相对最好，但绝对判别弱 |
| T21 | Random Forest | 0.593 | 0.076 | 0.0233 | 不支持树模型 |
| T21 | XGBoost | 0.518 | 0.033 | 0.0234 | 接近随机，不采用 |

这些数字只能说明候选方向，不能直接作为最终竞赛结果，原因包括：

- T13 仅 18 名阳性孕妇；
- T21 仅 12 名阳性孕妇；
- 单次划分对 AUC 影响很大；
- 尚需将质量门控、概率校准和阈值选择完整嵌套；
- 尚需配对区间而非只看点估计。

对随机森林更换 5 个孕妇分组随机种子后的探索性稳定性为：

| 标签 | ROC-AUC 均值（范围） | PR-AUC 均值（范围） | 解释 |
|---|---:|---:|---|
| T13 | 0.814（0.800—0.833） | 0.279（0.218—0.304） | 非线性候选信号最稳定 |
| T18 | 0.812（0.809—0.814） | 0.376（0.354—0.388） | ROC 稳定，但 PR 仍不优于统计基线的 0.407 |
| T21 | 0.489（0.352—0.569） | 0.043（0.030—0.057） | 明显不稳定，不支持树模型 |

这些“范围”只是少量随机划分的波动范围，不是置信区间；仍需完整嵌套配对 Bootstrap 才能晋级。

据此形成的明确取舍：

1. **T13**：随机森林是唯一值得重点检验的机器学习增强点；若配对区间通过门槛，可在正文突出“局部非线性显著改善”；
2. **T18**：保留正则逻辑，随机森林只作附录，因为 ROC 提升没有转化为 PR-AUC 和 Brier 改善；
3. **T21**：使用低维 Firth 和更宽复检区；模型不可识别时拒绝强判，不允许树模型或单 Z 规则接管；
4. **XGBoost**：从主线删除。

## 9.15 随机森林的受限晋级规则

若 T13 随机森林通过正式门槛，采用受限结构：

- 最大深度限制在 2—5；
- 叶节点至少包含若干名不同孕妇，而非若干条重复记录；
- 每棵树按孕妇而非记录采样；
- 训练时使用每孕妇等权；
- 只使用预先允许的特征；
- 外层训练折内完成超参数选择和校准；
- 使用测试折置换重要度，不用训练纯度重要度。

若它未通过门槛，则不做软投票、不做堆叠，直接保留统计模型。

## 9.16 模型分歧处理

假设统计模型和已晋级的 T13 随机森林都通过质量与校准要求，定义：

\[
\widehat d_{stat}\ne\widehat d_{RF}
\]

为模型分歧。分歧时：

\[
\boxed{\text{输出建议复检，而非平均概率后强判}}
\]

原因是两模型错误相关结构未知，小样本下简单平均没有理论上必然的收益。只有嵌套验证证明加权集成同时改善校准、PR-AUC 与选择性风险，才允许建立集成；本方案不预设这一额外层。

## 9.17 解释与个案报告

对每条异常或复检记录输出：

- 目标染色体 Z 值及距阈值的距离；
- 校准异常概率与 95% Bootstrap 区间；
- 样本质量等级和触发的质量标记；
- 统计模型中贡献最大的 2—3 项；
- 若为随机森林，使用外样本置换重要度的全局解释，不把单个 SHAP 值解释为因果；
- 是否超出训练 BMI、年龄或读段支持域；
- 建议：复检、遗传咨询或后续确证，而非直接替代临床诊断。

## 9.18 问题四最终输出模板

### 模型表现

| 标签 | 最终模型 | 阳性孕妇数 | PR-AUC | ROC-AUC | Brier | 敏感度 | 特异度 | 校准斜率 | 拒判率 | 是否有稳定 ML 增益 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| T13 | 待判 | 18 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待判 |
| T18 | 正则逻辑候选 | 30 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 初步否 |
| T21 | 低维 Firth/保守复检 | 12 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 待计算 | 否 |

### 个体判定

| 孕妇代码 | 记录号 | 质量等级 | \(p_{13}\) 与区间 | \(p_{18}\) 与区间 | \(p_{21}\) 与区间 | 最终结论 | 异常染色体 | 主要依据 | 建议 |
|---|---|---|---|---|---|---|---|---|---|
| 待输出 | 待输出 | 待输出 | 待输出 | 待输出 | 待输出 | 正常/复检/异常 | 待输出 | 待输出 | 待输出 |

## 9.19 问题四的叙事结论写法

建议正文表述为：

> 男胎问题通过 Y 浓度是否可靠达标决定“何时测”，女胎问题则先通过测序质量决定“这次结果能否判”，再通过染色体特异概率决定“判为何种异常”。两条分支都遵循可靠性约束优先、点预测次之、证据不足允许复检的原则。机器学习没有被整体叠加到所有标签上，而只在 T13 显示潜在稳定增益时申请局部晋级，从而兼顾预测能力、样本规模和可解释性。

---

# 10. 统一的不确定性、稳健性与外推分析

## 10.1 五层不确定性

本题的不确定性不能只用一个回归标准误概括，应拆成：

| 层级 | 来源 | 主要影响 | 处理方式 |
|---|---|---|---|
| 技术测量不确定性 | 同次采血复测波动、读段与 GC 质量 | 4% 状态、女胎 Z 信号是否可信 | 技术重复方差、可信状态、质量门控 |
| 个体生物不确定性 | 同一孕妇随孕周变化、孕妇间差异 | Y 浓度轨迹和达标时间 | 混合效应、事件时间分布 |
| 抽样不确定性 | 267 名男胎孕妇、147 名女胎孕妇的有限样本 | 系数、切点、性能和概率区间 | 孕妇簇 Bootstrap |
| 模型不确定性 | 线性/非线性、AFT 分布、分类器差异 | 时点与异常概率 | 候选分布、配对验证、模型审计 |
| 决策与外推不确定性 | \(\rho\)、阈值、代价、稀疏高 BMI | 推荐周、复检率、尾部风险 | 敏感性网格、拒绝外推、情景分析 |

五层不确定性分别计算，最后再观察它们对最终决策的联合影响。不能把所有方差简单相加后称为“总检测误差”。

## 10.2 整流程孕妇簇 Bootstrap

### 10.2.1 重抽样单位

男胎和女胎均按孕妇代码有放回抽样。同一孕妇的所有记录、访视、复测和标签整体进入一次 Bootstrap 样本。

### 10.2.2 每次重算内容

对第 \(b\) 次 Bootstrap：

1. 按孕妇重抽样；
2. 重新定义采血事件；
3. 重新聚合同次采血复测；
4. 重新估计 \(\sigma_{tech}^{(b)}\)；
5. 重新构造可信状态和删失区间；
6. 重新拟合 Q1 混合模型；
7. 重新选择 Q2 AFT 分布；
8. 重新计算个体可靠时点；
9. 重新运行精确动态规划；
10. 重新拟合 Q3 增量模型并应用晋级门；
11. 重新估计 Q4 质量中心、分类器、校准器和阈值；
12. 保存全部结果。

若只在固定预处理后的数据上重复拟合，会漏掉访视定义、误差估计、状态重构和分组优化带来的变异。

### 10.2.3 重复次数

- 路线调试：\(B=200\)；
- 正式区间：\(B=1000\)；
- 若计算量受限：至少 \(B=500\)，并报告 Monte Carlo 标准误。

### 10.2.4 男胎保存量

每次保存：

\[
\widehat\sigma_{tech}^{(b)},
\quad
\widehat\beta^{(b)},
\quad
\widehat\sigma_{AFT}^{(b)},
\quad
c_1^{(b)},\ldots,c_{K-1}^{(b)},
\quad
t_1^{(b)},\ldots,t_K^{(b)}.
\]

并保存：

- AFT 分布入选情况；
- 每个候选切点的入选频率；
- 每名孕妇的组归属；
- 每组覆盖率、平均概率和 CVaR；
- 不可保证孕妇数量；
- Q3 相对 Q2 的切点与时点变化。

### 10.2.5 女胎保存量

每个标签保存：

- 模型类型入选率；
- 变量入选率与系数方向；
- 校准截距和斜率；
- 决策阈值；
- 每条记录的概率分布；
- 正常/复检/异常频率；
- 性能指标和选择性风险。

## 10.3 男胎敏感性网格

不建议对所有组合做完全笛卡尔积。采用“单因素扰动 + 关键双因素组合”：

| 扰动项 | 主值 | 敏感性值 | 观察量 |
|---|---|---|---|
| 访视键 | 代码+抽血次数+孕周 | 再加入检测日期 | \(\sigma_{tech}\)、区间类型、切点 |
| Y 阈值 \(c\) | 0.040 | 0.038、0.042 | 达标区间、组时点 |
| 误差倍数 \(\kappa\) | 1.00 | 0.75、1.25、1.50 | 可信状态、不可保证比例 |
| 可靠度 \(\rho\) | 0.80、0.90 | 0.70、0.95 | 时点、覆盖、延迟 |
| AFT 分布 | log-normal | Weibull、log-logistic | 分位时点、AIC/NLL |
| BMI 形态 | 线性 | 单调折线 | 尾部时点、外样本得分 |
| 组数 \(K\) | 外层选 | 2—6 | 决策损失、可执行性 |
| 最小组规模 | 预设 | 15、20、30 | 切点稳定性 |
| 状态冲突 | 主规则 | 最早可信/保守右移 | 区间类型、时点 |
| 高 BMI 处理 | 全部保留 | 留一尾部孕妇、截尾主体 | 最高组时点与区间 |

重点双因素组合：

\[
(c,\kappa)
\in
\{(0.038,1.25),(0.040,1.50),(0.042,1.25)\},
\]

\[
(\rho,K)
\in
\{(0.80,3),(0.80,4),(0.90,3),(0.90,4)\}.
\]

## 10.4 分组稳定性的量化

### 10.4.1 切点稳定性

对第 \(k\) 个切点：

\[
Stab(c_k;\delta)
=\frac1B
\sum_{b=1}^{B}
\mathbf1\{|c_k^{(b)}-\widetilde c_k|\le\delta\}.
\]

建议：

\[
\delta=0.5\text{ BMI 单位}.
\]

若切点个数随 Bootstrap 改变，先匹配最近切点，再报告“缺失模态”频率。

### 10.4.2 推荐时点稳定性

\[
Stab(t_k;\Delta)
=\frac1B
\sum_b
\mathbf1\{|t_k^{(b)}-\widetilde t_k|\le\Delta\},
\]

建议 \(\Delta=0.5\) 周。

### 10.4.3 成员归属稳定性

计算任意两次 Bootstrap 分组的调整 Rand 指数，或对每名孕妇报告进入各组的频率。若相邻切点高度波动但组时点几乎相同，应优先合并两组。

### 10.4.4 可执行性规则

某一组只有同时满足以下条件才进入最终表：

- Bootstrap 中存在频率至少 70%；
- 推荐时点半周内稳定率至少 70%；
- 主分析严格覆盖率满足 \(\rho\)；
- 误差放大 25% 后不出现大规模失效；
- 取整后仍满足约束。

否则合并相邻组或标记为探索性分层。

## 10.5 女胎敏感性分析

| 扰动项 | 主分析 | 敏感性 | 重点观察 |
|---|---|---|---|
| 标签单位 | 记录级、孕妇分组 | 首次/最后/任一次阳性 | PR-AUC、Brier、阈值 |
| 质量门控 | 95%/99% 两级 | 97.5%、单项规则 | 复检率、低质量误报 |
| 插补 | 训练折中位数+缺失指示 | 多重插补 | 概率和变量入选 |
| 权重 | 每孕妇总权重 1 | 每记录等权 | 是否被高频复检孕妇主导 |
| 类别代价 | 预设主值 | 漏报代价 2、5、10 倍 | 敏感度与复检率 |
| 校准 | Logistic calibration | 不校准、收缩校准 | Brier、校准斜率 |
| Z 特征 | 线性+单折线 | 仅线性、固定阈值 | 稳定性和解释 |
| ML 候选 | 受限随机森林 | 更浅/更深树 | T13 增益是否可重复 |

若 T13 随机森林只在某一个标签口径、某一个质量阈值下优于统计模型，则不能称为稳健提升。

## 10.6 测量误差与模型残差的严格区分

男胎观测模型：

\[
Y_{ivr}^{obs}
=Y_{iv}^{*}+e_{ivr},
\qquad
Var(e_{ivr})=\sigma_{tech}^2.
\]

Q1 纵向模型残差：

\[
Y_{iv}^{*}
=m(t_{iv},x_i)+u_i+\epsilon_{iv},
\qquad
Var(\epsilon_{iv})=\sigma_{bio+model}^2.
\]

因此：

- \(\sigma_{tech}\) 来自同次采血复测，进入阈值可信状态；
- \(\sigma_{bio+model}\) 来自不同访视，包含生物变化和模型遗漏；
- 二者可以在明确的分层观测模型中联合，但不能在文字上互换；
- 若做压力测试：

\[
\sigma_{stress}
=\sqrt{\sigma_{tech}^2+\sigma_{bio+model}^2},
\]

必须说明这是保守预测噪声，不是纯检测误差。

女胎中，质量门控刻画的是测序结果的可判性；分类器概率区间刻画的是标签预测不确定性，两者同样不能混成一个分数。

## 10.7 外推支持域

### 10.7.1 单变量支持

对关键变量报告：

\[
[Q_{0.01},Q_{0.99}],
\quad
[min,max],
\quad
\text{局部样本密度}.
\]

高 BMI 尾部尤其单列。

### 10.7.2 多变量支持距离

在训练折内对标准化临床变量计算收缩 Mahalanobis 距离：

\[
D_{X,i}^2
=(x_i-\widehat\mu_x)^\top
\widehat\Sigma_x^{-1}
(x_i-\widehat\mu_x).
\]

定义：

- 核心支持域：不超过训练 95% 分位；
- 边界支持域：95%—99%；
- 支持域外：超过 99% 或任一关键变量超出训练极值。

### 10.7.3 支持域外输出

\[
\boxed{
\text{不返回貌似精确的单一推荐周或异常概率}
}
\]

而应输出：

- 边界支持：给宽区间 + 保守方案；
- 支持域外：无法保证 + 建议个体化复检/专业判断；
- 在正文明确“模型内稳健”不等于“跨地区、跨平台、跨人群可迁移”。

## 10.8 最坏情景压力测试

男胎最坏情景同时采用：

- Y 阈值 4.2%；
- 技术误差放大 1.5 倍；
- AFT 使用给出最晚上分位的候选分布；
- 高 BMI 尾部留一孕妇；
- 可靠度 \(\rho=0.90\)。

观察最终建议是否只是整体推迟，还是分组结构崩溃。若结构崩溃，应降低组数并扩大最高组复检区，而不是继续报告原切点。

女胎最坏情景同时采用：

- 低质量样本比例上升；
- 漏报代价提高；
- T21 阳性孕妇留一法；
- 标签使用最后记录或首次记录两种极端口径。

若某个模型的异常判断在压力测试中频繁翻转，则扩大复检区或回退到更简单模型。

---

# 11. 从数据到论文答案的完整技术路线

## 11.1 阶段 A：数据审计

1. 读取 Excel 两个工作表，保留原字段名和原始行号；
2. 建立字段字典、单位字典、结构性缺失字典；
3. 解析孕周为十进制周；
4. 核验日期、抽血次数、孕周与孕妇代码；
5. 建立主访视键与含日期敏感性访视键；
6. 统计技术重复、纵向重复、缺失、极端值和支持域；
7. 生成固定孕妇级折文件。

## 11.2 阶段 B：男胎统计主线

8. 合并同次采血复测并估计技术误差；
9. 拟合 Q1 组间/组内 BMI 混合模型；
10. 输出效应、显著性、ICC/随机效应和纵向达标概率；
11. 依据技术误差构造 Q2 可信状态；
12. 将每名孕妇压缩为首次可靠达标删失区间；
13. 比较 log-normal、Weibull、log-logistic AFT；
14. 计算稳健个体可靠时点；
15. 用精确动态规划得到 BMI 切点和组时点；
16. 对切点取整并重新核验覆盖；
17. 在 Q3 嵌套加入检测前多因素；
18. 应用增量晋级门；
19. 必要时重跑同一个 DP；
20. 完成误差、可靠度、分布和高 BMI 尾部敏感性。

## 11.3 阶段 C：女胎判定分支

21. 从 AB 拆分 T13、T18、T21 标签；
22. 建立非冗余质量向量和质量门控；
23. 建立三个统计分类器；
24. 在固定孕妇折内完成调参、校准和阈值选择；
25. 对 T13/T18 运行受限随机森林审计；
26. 应用逐标签晋级门；
27. 通过簇 Bootstrap 得到概率区间；
28. 形成正常/复检/异常三段式结果；
29. 完成标签口径、质量门控和小阳性敏感性。

## 11.4 阶段 D：统一复核和写作

30. 检查所有模型是否使用相同训练/测试孕妇；
31. 检查每一个最终数值能否追溯到数据和配置；
32. 生成模型比较、稳健性和最终决策表；
33. 生成 8 幅核心图；
34. 撰写四问衔接段和统一结论；
35. 将未晋级模型、完整参数和扩展敏感性移到附录；
36. 固化随机种子、软件版本、数据哈希和运行日志。

## 11.5 全流程伪代码

~~~text
读取原始附件
建立字段与缺失字典
解析孕周并生成两个访视键
按孕妇生成固定外层折

男胎:
    聚合同次采血技术复测
    估计技术误差
    拟合 Q1 混合效应模型
    for 每个可靠度与误差场景:
        构造可信达标状态
        形成每名孕妇的区间删失时间
        在训练折比较 AFT 分布
        计算稳健个体可靠时点
        对每个候选组数运行精确 DP
        取整切点并重新核验覆盖
    冻结 BMI-only 基线
    拟合 Q3 多因素 AFT
    if 配对增量通过预设门槛:
        用修正个体时点重跑 DP
    else:
        保留 Q2 分组和时点

女胎:
    拆分三个异常标签
    建立训练折质量中心和门控
    分别拟合 T13、T18、T21 统计模型
    校准概率并选择阈值
    对 T13/T18 运行受限 RF 审计
    if 某标签 RF 通过全部门槛:
        仅替换该标签模型
    用孕妇簇 Bootstrap 形成概率区间
    质量不足或区间跨阈值 -> 建议复检

汇总:
    生成最终表、稳健性表、图形和附录
    执行泄漏、覆盖、支持域与复现检查
~~~

---

# 12. 公平模型比较与复杂度控制

## 12.1 四问统一比较表

| 问题 | 简单基线 | 主候选 | 复杂候选 | 主评价指标 | 安全/稳健指标 | 最终规则 |
|---|---|---|---|---|---|---|
| Q1 | OLS/GEE | 组间/组内混合模型 | GAMM、RF | 新孕妇 RMSE/MAE | 覆盖率、效应稳定性 | 复杂候选需显著改善且方向稳定 |
| Q2 | 经验首次达标分组 | 区间删失 log-normal AFT + DP | 其他 AFT、纵向重构 | 区间 NLL、决策损失 | 严格覆盖、切点稳定、高 BMI | 先满足覆盖，再最小化损失 |
| Q3 | Q2 BMI-only 冻结模型 | 嵌套多因素 AFT | 区间 Cox、随机生存森林 | 配对 NLL/IBS | 校准、切点/CVaR | 增量区间通过才升级 |
| Q4 | Z 阈值规则 | 正则逻辑/Firth | T13/T18 受限 RF | PR-AUC、Brier | 敏感度、校准、选择性风险 | 每条染色体单独申请晋级 |

## 12.2 统一的词典序选择

模型选择不使用“把多个指标任意加权成一个总分”，采用：

1. 数据与统计单位正确；
2. 无信息泄漏；
3. 满足安全或可靠性约束；
4. 外样本主指标改善；
5. 校准和尾部表现不恶化；
6. 改善具有统计与实际意义；
7. 复杂度更低者优先；
8. 解释和落地更清楚者优先。

## 12.3 机器学习晋级门

机器学习只有同时满足下列条件才进入正文最终模型：

1. 与统计模型使用完全相同的外层孕妇折；
2. 全部预处理和调参嵌套在训练折；
3. 主指标的配对改善 95% 区间下界大于 0；
4. 改善超过预先指定的最小实际阈值；
5. 校准、敏感度、覆盖率或尾部风险无显著恶化；
6. 多次划分中改善方向稳定；
7. 变量重要性和局部响应不违反基本医学/统计常识；
8. 模型在支持域边界不产生荒谬外推；
9. 增益能改变一个有意义的最终决策，而非只改善第三位小数。

若只满足部分条件，定位为“辅助审计”，不称为“融合模型”。

## 12.4 复杂度预算

为避免方法堆砌，每问正文最多保留：

- 一个主统计模型；
- 一个最必要的分布/规则备选；
- 一个机器学习挑战者；
- 一个统一的 Bootstrap 稳健框架。

其余模型只在附录的公平比较表中出现，不逐个写长篇原理。

## 12.5 消融实验

### 男胎

| 消融版本 | 删除内容 | 目的 |
|---|---|---|
| A0 | 不处理技术重复 | 量化访视重构价值 |
| A1 | 不做可信状态，只硬阈值 | 量化测量误差价值 |
| A2 | 不做区间删失，用首次观测周 | 量化删失建模价值 |
| A3 | 不用 DP，手工 BMI 分组 | 量化优化器价值 |
| A4 | Q3 去除非 BMI 因素 | 即冻结 Q2 基线 |

### 女胎

| 消融版本 | 删除内容 | 目的 |
|---|---|---|
| B0 | 只用 Z 阈值 | 建立临床规则基线 |
| B1 | 删除质量变量 | 检查质量对误报和校准的贡献 |
| B2 | 删除拒判 | 检查强制二分类造成的风险 |
| B3 | 每记录等权 | 检查重复孕妇支配效应 |
| B4 | 删除临床变量 | 判断提升来自 Z/质量还是临床特征 |

消融结果比继续增加算法更能说明创新究竟来自哪里。

---

# 13. 核心创新点及其证据链

## 13.1 创新总表

| 创新点 | 解决的原始缺陷 | 数学实现 | 直接证据 | 最终决策价值 |
|---|---|---|---|---|
| 记录—采血事件—孕妇三级重构 | 把技术复测、纵向随访当独立样本 | 双访视键、组内误差、孕妇聚类 | 主/严格口径重复组与误差不同 | 修正显著性、阈值状态和验证 |
| 误差感知的首次可靠达标区间 | 首次越过 4% 后可能回落，简单首达时点不稳 | \(P(Y^*\ge0.04\mid Y^{obs})\) + 区间删失 | 32 人阳性后转阴、28 人多次转换 | 给 AFT 提供可信事件时间 |
| 生存分布到 BMI 精确分组 | 回归模型不能直接产生可执行切点 | 个体可靠分位 + 连续分段 DP | 修正上取整后严格满足覆盖 | 直接回答分组和最佳时点 |
| 冻结基线的多因素增量门 | 问题三容易模型堆砌并推翻问题二 | 嵌套 AFT + 配对 CV + CVaR | 初步多因素/ML 增益不稳定 | 有证据才更新，无证据保留 Q2 |
| 女胎质量—概率—拒判三层决策 | 低质量样本被强制正常/异常 | 非冗余质量距离 + 三分类器 + 概率区间 | T21 小阳性、标签变动、读段冗余 | 降低不可靠强判，给出复检建议 |
| 选择性机器学习晋级 | 统计与 ML 两线并行导致叙事分裂 | 同折配对改善区间、逐任务晋级 | Q1/Q3 无稳定增益，T13 有潜力 | 只突出真正改变结论的 ML |

## 13.2 最值得在摘要强调的三点

1. **误差感知的区间删失时点重构**：不是把第一次观测到 4% 的孕周当作真实达标时点；
2. **可靠度约束下的一维精确动态规划**：保证 BMI 分组连续、可执行且严格覆盖；
3. **质量感知的选择性异常判定**：不确定时允许复检，机器学习只逐标签晋级。

## 13.3 不宜包装成创新的内容

- 使用 Pearson/Spearman 相关；
- 使用随机森林或 XGBoost 本身；
- 使用常规 5 折交叉验证；
- 罗列多个 AFT 分布；
- 画 SHAP 图；
- 把多个模型简单平均。

这些只能作为实现组件，不能替代问题驱动的结构创新。

## 13.4 创新与故事线的连接

\[
\text{Q1 发现重复与误差}
\Rightarrow
\text{Q2 重构可靠达标时间}
\Rightarrow
\text{Q2 将时间分布压缩为 BMI 规则}
\Rightarrow
\text{Q3 检验规则是否需要多因素修订}
\Rightarrow
\text{Q4 将可靠性优先原则迁移到女胎判定}.
\]

这条链比“混合模型—AFT—随机森林—XGBoost”的算法名串联更像一篇完整的国赛论文。

---

# 14. 国赛论文的故事线与写作结构

## 14.1 推荐论文标题

主标题建议：

> **可靠性约束下的 NIPT 时点优化与质量感知异常判定**

可选副标题：

> ——基于纵向混合模型、区间删失 AFT 与选择性分类

标题突出问题和决策，不把随机森林等算法名放在最显眼位置。

## 14.2 摘要的四段式结构

### 第一段：问题与总思路

建议写法：

> 针对 NIPT 中男胎检测时点选择与女胎染色体异常判定问题，本文首先按“孕妇—采血事件—检测记录”重构重复数据，分离技术复测误差与个体差异；随后以“先满足可靠性，再优化时点或判定”为统一原则，分别建立男胎可靠达标时间模型和女胎质量感知分类模型。

### 第二段：问题一、二

建议写法：

> 对男胎数据，建立组间/组内 BMI 分解的混合效应模型，刻画孕周、BMI 对 Y 染色体浓度的作用并估计个体异质性。考虑 4% 阈值附近的检测误差和多次上下穿越，将每名孕妇的首次可靠达标时间表示为删失区间，建立区间删失 AFT 模型；再将个体可靠分位时点输入一维连续分段动态规划，在严格组覆盖约束下同时求得 BMI 切点和推荐孕周。

### 第三段：问题三、四

建议写法：

> 在问题三中冻结 BMI-only 方案，以嵌套多因素 AFT 和配对外样本验证检验身高、年龄等变量的增量价值；仅当预测、校准和尾部风险均稳定改善时才修订分组。在问题四中，先以非冗余测序质量指标识别不可可靠判定的样本，再对 T13、T18、T21 分别建立正则逻辑/Firth 模型，并用概率区间形成正常、复检、异常三段式决策；随机森林仅对显示稳定非线性增益的单个标签申请晋级。

### 第四段：主要结果与结论

这一段必须填入正式重算结果，不能复制快速试算。模板为：

> 结果表明，孕周对 Y 浓度的效应为【填入】，首次 BMI 的效应为【填入】，孕妇间差异占【填入】。在可靠度 \(\rho=\)【填入】下，最优 BMI 分组为【填入】，推荐时点依次为【填入】，Bootstrap 中切点半单位稳定率为【填入】。多因素模型相对 BMI-only 的外样本改善为【填入】，因此【保留/修订】问题二方案。女胎模型对 T13、T18、T21 的 PR-AUC 分别为【填入】，在复检率【填入】下选择性错误率为【填入】。敏感性分析显示【填入】，说明模型在观测支持域内具有【填入】的稳健性。

## 14.3 正文建议目录

1. 问题重述；
2. 问题分析；
3. 模型假设与符号说明；
4. 数据预处理与质量审计；
5. 问题一：Y 浓度纵向关系；
6. 问题二：可靠达标时点与 BMI 分组；
7. 问题三：多因素增量与稳健修订；
8. 问题四：质量感知女胎异常判定；
9. 模型检验、敏感性与外推边界；
10. 模型评价、优缺点与推广；
11. 结论；
12. 参考文献；
13. 附录。

“数据预处理”必须独立成节，因为三级统计单位重构是全题结论可靠的基础，不是普通清洗细节。

## 14.4 四问之间的过渡段

### Q1 到 Q2

> 问题一说明 Y 浓度总体随孕周变化、受 BMI 影响，且同一孕妇内的记录具有显著相关性；更重要的是，同次采血复测表明 4% 阈值附近存在不可忽略的技术波动。因此，问题二不能把第一次观测到 \(Y\ge4\%\) 的孕周直接视为真实达标时点，而需先把浓度关系转化为“首次可靠达标”的时间区间。

### Q2 到 Q3

> 问题二在题目限定下仅用 BMI 形成了基础分组，其优点是规则简洁、可执行；但同一 BMI 的孕妇仍可能因年龄、身高和孕产史产生不同的达标时间。问题三因此保留问题二的事件定义、可靠度和动态规划，只检验其他检测前因素能否稳定修正个体时点。

### Q3 到 Q4

> 前三问解决了含 Y 染色体信息时“何时检测最可靠”。女胎缺少 Y 信号，问题由时点优化转为多染色体异常判定，但可靠性优先原则仍然成立：应先判断测序质量是否足以支持结论，再给出异常概率；证据不足时，与男胎高 BMI 尾部一样保留复检或个体化处理出口。

### 全文结论过渡

> 由此，四问并非四个孤立算法，而是从数据可靠性识别、达标时间估计、群体规则压缩，到多标签质量感知决策的一条完整链路。

## 14.5 建议保留的 8 幅核心图

| 图号 | 内容 | 服务的问题 | 必须表达的信息 |
|---|---|---|---|
| 图 1 | 全题“数据—模型—决策”流程图 | 全文 | 四问接口和统计/ML 主辅关系 |
| 图 2 | 男胎 BMI、孕周、Y 分布及支持密度 | Q1—Q3 | 主体区间与高 BMI 稀疏尾部 |
| 图 3 | Q1 典型 BMI 下 Y—孕周效应曲线 | Q1 | 固定效应、95% 区间、4% 阈值 |
| 图 4 | AFT 达标分布与纵向概率校准对照 | Q1—Q2 | 两种模型在关键孕周的一致与分歧 |
| 图 5 | BMI 排序后的个体可靠时点与 DP 分段 | Q2 | 切点、组时点、未覆盖成员 |
| 图 6 | 切点/时点 Bootstrap 分布和敏感性热图 | Q2—Q3 | 稳定性、\(\rho\)、误差影响 |
| 图 7 | 女胎质量空间与三标签概率校准图 | Q4 | 质量门控和概率可信度 |
| 图 8 | PR 曲线与选择性风险—覆盖曲线 | Q4 | 小阳性表现和拒判收益 |

不要在正文连续堆放多张 ROC、SHAP、相关热图。只有能支撑一个模型选择或最终决策的图才进入正文。

## 14.6 建议保留的 9 张核心表

| 表号 | 内容 |
|---|---|
| 表 1 | 符号与统计单位 |
| 表 2 | 数据审计、缺失、重复和支持域 |
| 表 3 | Q1 候选模型与外样本表现 |
| 表 4 | AFT 分布、参数、区间得分与关键分位 |
| 表 5 | Q2 最终 BMI 分组、时点、覆盖率与区间 |
| 表 6 | Q3 多因素增量比较 |
| 表 7 | Q2—Q3 分组变化与高 BMI 尾部 |
| 表 8 | Q4 三标签模型表现、校准和拒判率 |
| 表 9 | 稳健性、外推边界和最终建议汇总 |

## 14.7 正文与附录分工

### 正文保留

- 数据层级和关键审计事实；
- 每问一个主模型和一个必要备选；
- 关键公式；
- 模型公平比较结论；
- 最终切点、时点、异常性能；
- 机器学习是否晋级及其证据；
- 主要敏感性和限制。

### 附录保留

- 完整字段字典；
- 全部缺失统计；
- 所有候选 AFT 参数；
- 全部 Bootstrap 分布；
- 不同 \(\rho\)、阈值、质量门控的完整表；
- 随机森林超参数和置换重要度；
- 未晋级模型结果；
- 关键代码或伪代码；
- 额外残差图、Q-Q 图和诊断。

## 14.8 结果措辞规则

### 可以写

- “在本附件样本和观测支持域内……”；
- “配对外样本改善的 95% 区间为……”；
- “未发现足够证据支持复杂模型替换基线……”；
- “该组达到预设可靠度约束……”；
- “对支持域外样本建议个体化复检……”。

### 不应写

- “模型证明 BMI 导致 Y 浓度下降”；
- “AIC 最小所以一定预测最好”；
- “随机森林准确率最高，说明机器学习更先进”；
- “高 BMI 孕妇在 16.8 周一定达标”；
- “女胎异常模型可以替代临床诊断”；
- “敏感性分析稳定，因此可以推广到所有地区和测序平台”。

## 14.9 论文中的主辅线比例

建议正文篇幅大致为：

- 数据审计与统一口径：15%；
- Q1：15%；
- Q2：25%；
- Q3：15%；
- Q4：20%；
- 稳健性、评价与结论：10%。

统计主线占模型论述约 75%—85%。机器学习只占 15%—25%，且主要集中于公平挑战、T13 潜在增强和“不晋级”的证据。这样主线突出而不排斥有效的机器学习。

---

# 15. 代码实现、输出文件与复现规范

## 15.1 推荐目录

~~~text
nipt_project/
├── data/
│   ├── raw/附件.xlsx
│   ├── interim/male_visit.csv
│   ├── interim/female_record.csv
│   └── processed/fold_id.csv
├── config/
│   ├── main.yaml
│   └── sensitivity.yaml
├── src/
│   ├── 00_data_dictionary.py
│   ├── 01_audit_and_visit_key.py
│   ├── 02_preprocess.py
│   ├── 10_q1_mixed_model.py
│   ├── 20_q2_interval_aft.py
│   ├── 21_q2_dynamic_programming.py
│   ├── 30_q3_incremental_aft.py
│   ├── 40_q4_quality_gate.py
│   ├── 41_q4_classifiers.py
│   ├── 50_cluster_bootstrap.py
│   ├── 60_sensitivity.py
│   └── 70_make_figures_tables.py
├── outputs/
│   ├── tables/
│   ├── figures/
│   ├── predictions/
│   ├── models/
│   └── logs/
├── tests/
│   ├── test_visit_key.py
│   ├── test_interval.py
│   ├── test_dp_coverage.py
│   ├── test_group_split.py
│   └── test_no_leakage.py
└── run_all.py
~~~

现有附件审计脚本可作为 01 脚本的原型，正式代码应拆分为可测试函数。

## 15.2 主配置文件

~~~yaml
seed: 20250904
male:
  y_threshold: 0.040
  reliability_levels: [0.80, 0.90]
  error_multiplier: 1.00
  aft_distributions: [lognormal, weibull, loglogistic]
  group_number_candidates: [2, 3, 4, 5, 6]
  minimum_group_size: 20
  cut_rounding: 0.5
  robust_alpha: 0.10
female:
  outer_folds: 5
  outer_repeats: 20
  quality_warn_quantile: 0.95
  quality_retest_quantile: 0.99
  bootstrap_repeats: 1000
  ml_candidates: [random_forest]
promotion:
  confidence_level: 0.95
  minimum_relative_nll_gain: 0.01
  minimum_brier_gain: 0.002
~~~

所有敏感性参数从配置读取，不能散落在代码中。

## 15.3 关键中间文件

### male_visit.csv

至少包含：

- 孕妇代码；
- 访视编号与主/严格访视键；
- 孕周；
- 首次 BMI、当前 BMI、身高、年龄；
- 聚合 Y 浓度；
- 技术复测次数、组内方差、极差；
- 读段质量；
- 可信达标概率和状态；
- 外层折。

### male_interval.csv

至少包含：

- 孕妇代码；
- \(L_i,R_i\)；
- 删失类型；
- 首次 BMI 和检测前协变量；
- 状态冲突标记；
- 是否落在支持域边界；
- 外层折。

### female_features.csv

至少包含：

- 孕妇代码、原始行号、记录序号；
- 三个标签；
- 三个目标 Z 值、X 指标；
- 非冗余质量变量；
- 临床变量；
- 每孕妇权重；
- 外层折。

## 15.4 最终结果文件

| 文件 | 内容 |
|---|---|
| q1_effects.csv | 固定效应、Bootstrap 区间、随机效应和检验 |
| q1_oof_predictions.csv | 新孕妇外层预测与区间 |
| q2_aft_comparison.csv | 三种 AFT 的参数、得分、校准 |
| q2_group_policy.csv | BMI 切点、推荐周、覆盖、风险、区间 |
| q2_bootstrap_stability.csv | 切点和时点入选频率 |
| q3_increment_test.csv | BMI-only 与多因素配对差异 |
| q3_group_policy.csv | 保留或修订后的最终男胎方案 |
| q4_oof_probabilities.csv | 三标签外层概率与模型类型 |
| q4_model_comparison.csv | 阈值、统计模型、RF 的公平比较 |
| q4_decisions.csv | 质量、概率区间、三段式结论 |
| sensitivity_summary.csv | 所有关键扰动的最终决策变化 |

## 15.5 随机性与版本

运行日志必须记录：

- 原始 Excel 文件 SHA-256；
- 代码提交哈希或压缩包版本；
- Python/R 与主要包版本；
- 操作系统；
- 随机种子；
- 固定外层折文件哈希；
- 配置文件原文；
- 开始/结束时间；
- 收敛警告和失败 Bootstrap 次数。

Bootstrap 和交叉验证的种子采用：

\[
seed_b=seed_0+b,
\]

且并行执行不能改变结果。

## 15.6 数值精度

- 内部计算保留双精度；
- BMI 切点表保留 2 位，落地规则取 0.5 或整数；
- 孕周内部保留至少 4 位，最终转为“周+天”；
- 概率保留 3 位；
- p 值小于 \(10^{-4}\) 用科学计数法；
- 置信区间与点估计使用相同小数位；
- 覆盖率使用上取整顺序统计量，不能用线性插值后宣称严格覆盖。

孕周转周+天：

\[
w=\lfloor t\rfloor,
\qquad
d=\operatorname{round}(7(t-w)).
\]

若 \(d=7\)，则令 \(w\leftarrow w+1,d\leftarrow0\)。

## 15.7 运行失败处理

- 混合模型奇异：删除随机斜率，保留随机截距；
- AFT 某分布不收敛：记录失败，不用随意换初值直到“赢”；
- Bootstrap 无可行 DP：记录不可行，进入不稳定率；
- 某外层折无阳性：重新生成分层孕妇折，不将指标记为 0；
- Firth 仍不收敛：停止 T21 自动强判，保留质量门控和临床参照并扩大复检区；
- 随机森林校准失败：不晋级；
- 支持域外：返回拒判，不截到训练边界后冒充有效预测。

---

# 16. 完整性审查与防断链检查

## 16.1 逐问题题意覆盖

| 题目要求 | 本方案对应位置 | 是否形成数值输出 | 断链风险 | 处理 |
|---|---|---|---|---|
| Q1 相关特性 | 混合模型效应曲线与检验 | 是 | 行级显著性虚高 | 孕妇聚类、Bootstrap |
| Q1 BMI 等因素 | 首次 BMI、组内变化、候选临床变量 | 是 | 决策后信息泄漏 | Q2/Q3 只用基线 |
| Q2 BMI 分组 | 一维连续 DP | 是 | 手工切点或空组 | 精确递推、最小组规模 |
| Q2 最佳时点 | 可靠分位和组覆盖 | 是 | 分位数插值违约 | 使用 \(\lceil\rho n\rceil\) |
| Q2 检测误差 | 技术重复、可信状态、多重扰动 | 是 | 与模型残差混淆 | 分层定义 |
| Q3 多因素 | 嵌套多因素 AFT | 是 | 方法堆砌 | 冻结基线与晋级门 |
| Q3 达标比例 | \(C_G(t;\rho)\)、\(\bar p_G(t)\) | 是 | 只报均值掩盖个体 | 两者同时报告 |
| Q4 AB 为结果 | 三标签构造 | 是 | 合成单标签丢类型 | 独立多标签 |
| Q4 综合指标 | Z、X、质量、临床变量 | 是 | 高共线、泄漏 | 非冗余与训练折处理 |
| Q4 判定方法 | 质量门控+概率区间+三段式输出 | 是 | 强制二分类 | 允许复检 |

## 16.2 跨问接口检查

| 上一问输出 | 下一问实际使用方式 | 检查 |
|---|---|---|
| Q1 技术误差 | Q2 可信状态和扰动 | 必须用同一访视键估计 |
| Q1 孕周/BMI 方向 | Q2 AFT 方向与概率曲线校验 | 只校验，不重复计入两次 |
| Q2 删失区间 | Q3 原样复用 | 不得重新定义以迎合多因素 |
| Q2 BMI-only 模型 | Q3 冻结公平基线 | 分布与折固定 |
| Q2 DP | Q3 只替换个体时点输入 | 损失和约束固定 |
| Q3 可靠性原则 | Q4 质量门控与拒判 | 是方法论连接，不共享无关参数 |

## 16.3 必须自动化的断言

### 数据断言

~~~text
每条记录恰好属于一个孕妇
每个技术复测组只对应一个孕妇和一个采血事件
同一孕妇不能跨外层折
训练折统计量不得由全数据预先计算
女胎 Y 空值不插补
AB 派生字段不得进入特征
~~~

### 区间断言

\[
0\le L_i<R_i\le\infty.
\]

- 左删失必须有 \(L_i=0\)；
- 右删失必须有 \(R_i=\infty\)；
- 无法解决的状态冲突必须有标记；
- 不得把右删失强制写成 25 周精确事件。

### DP 断言

对最终每组：

\[
n_G\ge n_{min},
\]

\[
\frac1{n_G}
\sum_{i\in G}
\mathbf1\{\widetilde t_i^{(\rho)}\le t_G\}
\ge\rho,
\]

且 BMI 区间连续、不重叠、覆盖所有纳入孕妇。

组时点索引：

\[
j_G=\lceil\rho n_G\rceil,
\]

不得使用：

\[
j_G=\rho(n_G-1)+1
\]

的线性插值分位后声称严格成员覆盖。

### 分类断言

- 三个概率均在 \([0,1]\)；
- 异常标签可同时为 1；
- 质量门控先于异常输出；
- 复检记录不得又被标为确定正常；
- 阈值不得使用外层测试标签选择；
- 每孕妇权重之和为 1；
- 模型比较使用相同测试记录。

## 16.4 数值健康检查

| 模块 | 健康信号 | 红旗 |
|---|---|---|
| Q1 混合模型 | 随机效应方差有限、无奇异警告 | 随机斜率相关接近 \(\pm1\) |
| AFT | 三分布参数有限、区间概率非零 | 大量 \(\log0\)、尺度趋近 0 |
| DP | 可行组数稳定、覆盖满足 | 取整后违约、单人尾部组 |
| Q3 | 增量方向多折一致 | 训练显著、测试恶化 |
| Q4 Logistic | 校准斜率接近 1 | 完全分离、概率全为 0/1 |
| Q4 RF | 多重复划分仍改善 | 单次 AUC 很高、区间极宽 |
| Bootstrap | 大多数重抽样成功 | 失败率超过 5% |

若 Bootstrap 失败率超过 5%，必须报告原因并简化模型，不能只丢弃失败样本。

## 16.5 真实性检查

全文数字分三类：

1. **附件复核**：原始计数、相关、技术误差等；
2. **快速试算**：用于确认路线可行，必须明确非最终结果；
3. **正式结果**：完整嵌套验证、误差感知状态、Bootstrap 和取整复核后得到。

写作前执行全文搜索，禁止把“快速试算”表中的数字直接复制到摘要最终结果，除非正式流水线复算后完全一致。

## 16.6 删除清单

最终论文主线应删除：

- 原始行级 OLS p 值作为正式显著性；
- 以检测日期定义同次采血的唯一口径；
- 第一次观测超过 4% 即精确事件；
- 线性插值分位数下的虚假 80% 覆盖；
- 25 周截断后的“保证达标”；
- Q3 假想检测失败监督模型；
- Q3 无显著增益的机器学习融合；
- Q4 XGBoost 主线；
- T21 树模型强判；
- GC 0.4—0.6 以外全部删除；
- 统计与 ML 概率无验证平均；
- 支持域外精确外推。

---

# 17. 最终推荐方案与条件化结论

## 17.1 推荐模型组合

| 模块 | 最终推荐 | 为什么 | 何时替换 |
|---|---|---|---|
| 数据结构 | 主访视键 + 含日期敏感性 | 最符合“一次采血多次检测” | 若题目或实验室明确检测日期代表独立采血 |
| Q1 | 组间/组内 BMI 简洁混合模型 | 处理重复、可检验、可传递误差和概率 | 明显残余曲率且 GAMM 外样本稳定改善 |
| Q2 时间模型 | 误差感知区间删失 log-normal AFT | 简洁、关键分位与其他分布接近 | 其他分布外层 NLL和校准显著更好 |
| Q2 决策 | 严格可靠度约束的精确 DP | 直接给连续 BMI 切点和时点 | 不替换，仅在不可行时减小组数 |
| Q3 | 嵌套多因素 AFT 增量检验 | 与 Q2 完整嵌套 | 只有配对增量通过才采用修订 |
| Q4 T13 | 正则逻辑基线，RF 条件晋级 | 快速试算 RF 有潜力但样本小 | 完整配对区间通过则突出 RF |
| Q4 T18 | 正则逻辑 | PR-AUC 与 Brier 更优 | RF 同时改善主指标和校准 |
| Q4 T21 | 低维 Firth/保守复检 | 阳性过少，且单 Z 规则在附件上灵敏度为 0 | 更大独立样本支持后再升级 |

## 17.2 统计主线与机器学习辅助线

最终不是两条同等重量的平行线，而是：

\[
\boxed{
\text{统计决策主线}
\xleftarrow{\text{挑战与审计}}
\text{选择性机器学习辅助线}
}
\]

具体含义：

- Q1：RF 检查浓度关系是否遗漏非线性；
- Q2：不用 ML 替代区间 AFT 和 DP；
- Q3：RF 检查多因素非线性增量，但当前证据不足；
- Q4：RF 只在 T13 可能改变最终模型；
- 任何 ML 未晋级都不算失败，而是支持更简洁可外推的方案。

## 17.3 若正式重算与快速复核一致

应形成如下结论：

1. Q1 保留简洁混合模型，不强调仅约 1.35% 的 RF RMSE 点改善；
2. Q2 以 log-normal AFT 为主，log-logistic/Weibull 作为分布敏感性；
3. Q2 使用修正后的上取整分位，原 80% 线性插值结果不得保留；
4. Q3 不用多因素 ML 替换 BMI-only 主模型，最多给尾部个体化提示；
5. Q4 重点正式复核 T13 随机森林；
6. Q4 T18 使用正则逻辑，T21 使用低维 Firth 与更宽复检区；
7. 最高 BMI 稀疏尾部和支持域外样本不得给强保证。

## 17.4 若 T13 随机森林正式晋级

论文可以把它作为唯一重点机器学习亮点：

> 在三类女胎异常中，机器学习增益具有明显任务异质性。受限随机森林只在 T13 上同时改善 PR-AUC、Brier 和固定敏感度下的特异度，且配对改善区间排除 0，因此替换 T13 的线性分类器；T18 与 T21 未达到同一证据门槛，继续使用更稳健的统计模型。

这种写法比“本文融合多种机器学习算法”更准确，也更有辨识度。

## 17.5 若 T13 随机森林未晋级

正文应直说：

> 随机森林在单次快速划分中显示局部优势，但在孕妇级重复验证和概率校准后改善区间覆盖 0，故不改变最终判定器。该结果说明当前样本不足以支持复杂非线性模型，最终采用正则统计模型和拒判机制控制泛化风险。

## 17.6 最终男胎策略的形式

最终策略是一个带适用域的函数：

\[
\Pi_{male}(BMI,x)
=
\begin{cases}
(G_k,t_k,\text{常规检测}),
&BMI\in[c_{k-1},c_k),
\ x\in\mathcal S_{core},\\
(G_k,t_k^{upper},\text{建议复核}),
&x\in\mathcal S_{boundary},\\
(\text{不强分组},\text{个体化时点},\text{无法保证}),
&x\notin\mathcal S.
\end{cases}
\]

其中 Q3 未晋级时 \(x\) 只用于支持域与提醒；Q3 晋级时 \(x\) 修正个体时点，但最终仍按 BMI 区间落地。

## 17.7 最终女胎策略的形式

\[
\Pi_{female}(r)
=
\begin{cases}
\text{建议复检},&Q_r\text{ 不足},\\
\text{异常}+\mathcal K_r,
&\exists k:L_{r,k}\ge\tau_k,\\
\text{正常},
&\forall k:U_{r,k}<\tau_k,\\
\text{建议复检},&\text{其余},
\end{cases}
\]

其中：

\[
\mathcal K_r
=\{k:L_{r,k}\ge\tau_k\}
\]

为异常染色体集合。

## 17.8 最终答案必须给出的数值

提交前不能缺少：

- Q1 最终模型形式、系数/效应、区间、显著性、个体差异；
- 技术误差及访视键敏感性；
- Q2 AFT 参数与分布比较；
- Q2 每个 BMI 组的边界、人数、时点、覆盖率和区间；
- Q2 \(\rho=0.80\) 与 0.90 两档；
- Q3 增量差异及其置信区间；
- Q3 保留/修订后的最终表；
- 高 BMI 不可保证范围；
- Q4 三标签的性能、校准、阈值和拒判率；
- Q4 每条记录的最终判定文件；
- 关键敏感性与外推警告。

---

# 18. 模型优缺点、适用范围与限制

## 18.1 优点

1. 正确区分记录、采血事件和孕妇三个统计层级；
2. 把技术误差真正传递到阈值状态和达标时间；
3. 区间删失避免伪造精确事件时点；
4. 动态规划直接回答 BMI 区间和最佳时点；
5. Q3 与 Q2 嵌套，复杂度由证据而非题号决定；
6. 高 BMI 尾部有显式拒绝外推机制；
7. 女胎采用多标签而非丢失异常类型；
8. 质量门控、校准概率和拒判降低不可靠强判；
9. 所有模型使用孕妇级固定折，可做公平配对比较；
10. 机器学习只在有显著实际增益处突出。

## 18.2 局限

### 数据代表性

附件来自特定地区或机构，且 BMI 分布偏高。内部稳健不能替代外部队列验证。

### 4% 阈值

4% 是题目给出的可靠性替代指标，不等于临床结局。模型优化的是“Y 浓度可靠达标时间”，不是直接最小化胎儿健康损失。

### 访视定义

附件未完全明确不同检测日期是否总是技术复测。主/严格双口径能量化影响，但不能替代实验室流程记录。

### 状态上下穿越

即使引入可信状态，有限访视仍只能把真实达标时间限制在区间内，不能观察连续轨迹。

### 高 BMI 尾部

BMI 40 以上仅 4 名孕妇，42 以上仅 2 名。任何精细尾部分组都缺乏稳定性。

### Q3 因果解释

多因素 AFT 是预测增量模型，不能将年龄、身高等系数解释为因果效应。

### 女胎标签

AB 是题目给定的检测判定结果，未必是出生结局或侵入性诊断金标准。模型可能学习现有实验室规则，而不是独立临床真相。

### 小阳性

T13、T21 的阳性孕妇很少，PR-AUC、校准和变量重要性区间会很宽。拒判机制只能控制强判风险，不能创造信息。

### 决策代价

题目没有给出精确的漏报、误报、复检和延迟代价，因此只能报告多档策略，不能声称某一阈值是唯一临床最优。

## 18.3 适用范围

模型适用于：

- 与附件测序平台、字段和人群相近的数据；
- 题目给出的 10—25 周检测窗口；
- 观测 BMI、年龄、读段和质量支持域内；
- 以 4% 为男胎可靠阈值；
- 以 AB 为女胎监督标签的算法评价。

模型不直接适用于：

- 不同测序平台且未重新校准；
- 明显低 BMI 或极端高 BMI 的支持域外人群；
- 多胎妊娠、特殊染色体结构异常等附件未覆盖情形；
- 将模型输出直接当作确诊；
- 未经伦理和临床验证的自动化医疗决策。

## 18.4 推广步骤

若迁移到新机构：

1. 先复算访视键和技术误差；
2. 检查协变量漂移和支持域；
3. 仅重校准截距/基线分布；
4. 再检查系数和切点是否需要更新；
5. 用本地孕妇级验证评价；
6. 女胎重新校准质量门控与概率；
7. 通过外部前瞻验证后再改变临床流程。

---

# 19. 提交物、材料来源与最终核对清单

## 19.1 本方案使用的本地材料

- 赛题原文：C题.pdf；
- 原始数据：附件.xlsx；
- 用户提供的原始机器学习辅助方案；
- 附件复核脚本及其数值审计结果；
- 本终稿中的模型、验证、稳健性与写作重构。

文档中的赛题说明用于理解问题与变量，不视为覆盖用户关于“连贯、精简、稳健、外推和准确性”的要求；最终方案取舍以用户要求和实际数据证据为准。

## 19.2 论文提交物

- 完整论文 PDF；
- 最终 Markdown/LaTeX 源文；
- 可运行代码；
- 原始数据只读副本；
- 配置文件；
- 固定折文件；
- 最终表格 CSV；
- 最终图形；
- 外层预测；
- Bootstrap 稳定性结果；
- 运行环境和日志。

## 19.3 提交前逐项勾选

### 数据

- [ ] 原始文件未被覆盖；
- [ ] 字段、单位、结构性缺失已说明；
- [ ] 主访视键与严格访视键均复算；
- [ ] 孕妇级固定折已保存；
- [ ] 同一孕妇未跨训练/测试。

### Q1

- [ ] 使用访视级聚合而非原始行级独立回归；
- [ ] 区分首次 BMI 与组内 BMI 变化；
- [ ] 检验了非线性和交互，但只保留有证据的结构；
- [ ] 新孕妇预测未使用其随机效应；
- [ ] 报告原尺度误差和覆盖率。

### Q2

- [ ] 技术误差进入可信状态；
- [ ] 所有孕妇都有合法删失区间；
- [ ] 三种 AFT 在相同折比较；
- [ ] 动态规划使用上取整分位；
- [ ] 取整后重新核验覆盖；
- [ ] 右删失和高 BMI 尾部允许“无法保证”；
- [ ] 报告 \(\rho=0.80\) 和 0.90。

### Q3

- [ ] BMI-only 基线被冻结；
- [ ] 只使用检测前可得变量；
- [ ] BMI/体重/身高共线性已处理；
- [ ] 配对增量有置信区间；
- [ ] 无显著增益时明确保留 Q2；
- [ ] 尾部 CVaR 和失败情景已报告。

### Q4

- [ ] AB 拆成三个可同时为 1 的标签；
- [ ] 质量门控不由 AB 训练；
- [ ] 总读段与唯一读段未重复堆叠；
- [ ] 每孕妇总权重为 1；
- [ ] T21 使用小样本稳健模型；
- [ ] 阈值和校准完全嵌套；
- [ ] 输出正常/复检/异常；
- [ ] RF 只逐标签晋级；
- [ ] XGBoost 未进入主线。

### 写作

- [ ] 摘要只写正式结果；
- [ ] 每问开头说明继承的输入，结尾说明传出的结果；
- [ ] 正文模型数受控；
- [ ] 关键图表直接支撑结论；
- [ ] 所有快速试算有明确标签；
- [ ] 不作因果或临床确诊过度表述；
- [ ] 限制和适用域单列。

## 19.4 一页式最终叙事

本题首先面对的并不是选择哪一种高级算法，而是如何从重复检测中恢复正确的统计单位。通过按孕妇、采血事件和检测记录分层，技术复测提供了 4% 阈值附近的测量误差，而纵向访视揭示了孕妇间显著异质性。问题一据此建立混合效应模型，说明 Y 浓度与孕周、BMI 的总体关系，并把技术误差与纵向概率传给问题二。

问题二把“第一次看到 4%”改写为“第一次可靠达到 4%”。由于真实达标发生在两个访视之间，使用区间删失 AFT 估计达标时间分布；再把每名孕妇的可靠分位时点输入精确动态规划，在严格覆盖约束下形成连续 BMI 分组和最早可行检测时点。这样，统计估计不再停留在系数层面，而是转化为题目要求的可执行策略。

问题三保持上述事件定义、可靠度和优化器不变，只检验年龄、身高等变量是否带来稳定增量。增量不足时保留问题二，增量充分时只修正有证据的时点和切点；高 BMI 稀疏尾部用 CVaR、宽区间和无法保证出口处理。由此，复杂模型不会破坏前问，而是以嵌套方式增强前问。

问题四虽然不再使用 Y 染色体，但延续同一个可靠性优先原则。先以非冗余读段与 GC 指标判断样本是否可判，再分别估计 T13、T18、T21 异常概率；概率不确定或质量不足时建议复检。机器学习只在某个标签具有稳定配对增益时局部晋级，避免把小样本上的偶然提升扩散到整套模型。

最终形成的不是四个互不相关的算法，而是一条：

\[
\boxed{
\text{数据可靠性}
\rightarrow
\text{关系识别}
\rightarrow
\text{时间分布}
\rightarrow
\text{群体决策}
\rightarrow
\text{多因素稳健化}
\rightarrow
\text{质量感知判定}
}
\]

的完整建模故事。

---

# 20. 结论性方案摘要

本终稿建议以统计模型为唯一主线：

\[
\text{混合效应模型}
\rightarrow
\text{误差感知区间删失 AFT}
\rightarrow
\text{可靠度约束动态规划}
\rightarrow
\text{嵌套多因素 AFT}
\]

并把机器学习限定为两个证据审计点：

1. Q1/Q3 检查是否遗漏稳定非线性；
2. Q4 的 T13 随机森林逐标签申请晋级。

如果正式嵌套验证仍与快速复核一致，应突出：

- 技术重复和阈值上下穿越的修正；
- 动态规划严格覆盖的修正；
- 多因素与机器学习没有稳定增益时保留简单模型；
- T13 可能是唯一值得强调的机器学习增强；
- 高 BMI、T21 和低质量样本必须允许保守复检或拒绝强外推。

这套结构在模型外推、稳健性、答案准确性、复杂度控制和四问连贯性之间形成了最清晰的平衡。
